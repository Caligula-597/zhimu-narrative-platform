# L1 验收更新

最后更新：2026-07-02

## L1-01 remaining bridges

- 已完成：`window.zhimuWorkspace` 在 `src/` 中的消费点已清除，`workspace-store.js` 改为直接导出业务 helper。
- 已完成：`window.zhimuRuntimeStore` 在 `src/` 中的消费点已清除，`runtime-store.js` 改为直接导出运行态 helper。
- 已完成：`window.zhimuFormat` 在 `src/` 中的消费点和发布桥已清除，`src/utils/format.js` 只保留 ES Module 导出。
- 已完成：`window.zhimuModal` 在 `src/` 中的消费点和发布桥已清除，`src/components/modal.js` 只保留 ES Module 导出。
- 已完成：`window.zhimuUi` 在 `src/` 中的消费点和发布桥已清除，`src/components/emptyState.js` 只保留 ES Module 导出。
- 已完成：新增 `npm run check:source-encoding`，用于验证 `src/**/*.js` 可被 UTF-8 fatal decoder 读取，并逐文件执行 `node --check`。
- 已完成：新增 `npm run migrate:format-bridge`，迁移脚本使用 Node UTF-8 读写和迁移前后语法检查，避免 Windows PowerShell 文本重写污染中文源码。
- 已完成：新增 `npm run migrate:modal-bridge`，按同样的 UTF-8 安全方式迁移 modal 消费者。
- 已完成：新增 `npm run migrate:ui-bridge`，按同样的 UTF-8 安全方式迁移 UI helper 消费者。
- 已完成：`window.zhimuUiSemantics` 在 `src/` 中的消费点和发布桥已清除，`src/components/ui-semantics.js` 只保留 ES Module 导出；`overview / clues / director / player` 已改为直接 import。
- 已完成：`scripts/verify-script-load.mjs` 增加 `zhimuUiSemantics` 反回归扫描，禁止 `src/` 重新引入该 window helper。
- 已完成：`window.zhimuCollapsePanel` 在 `src/` 中的消费点和发布桥已清除，`src/components/collapse-panel.js` 改为纯 ES Module；`writer / director / actions-workspace` 已改为直接 import。
- 已完成：`scripts/verify-script-load.mjs` 反回归扫描扩展到 `zhimuCollapsePanel`。
- 已完成：`window.zhimuStatus` 在 `src/` 与 `app.js` 中的消费点和发布桥已清除，`src/components/status-ui.js` 改为纯 ES Module 导出 `normalizeError / renderState / loading / empty / error / modalLoading / modalError` 共 6 个函数；19 个消费者全部改为直接 import：
  - Type A（12 文件，`normalizeError` 单函数 import）：`src/views/archive.js`、`src/views/assets.js`、`src/views/mini-games.js`、`src/runtime/actions-ops.js`、`src/views/pipeline-wizard-open.js`、`src/views/rules.js`、`src/views/settings.js`、`src/views/studio.js`、`src/views/writer.js`、`src/runtime/auth-world.js`、`src/runtime/wizard.js`、`src/runtime/data.js`
  - Type B（3 文件，`import * as Status` namespace）：`src/views/account-hub.js`、`src/views/account.js`、`src/runtime/global-search.js`
  - Type C（2 文件，`renderState` 单函数 import）：`src/components/service-outage.js`、`src/runtime/dependency-guard.js`
  - Type D（1 文件）：`src/components/ui-semantics.js`（`normalizeError` import）
  - 入口：`app.js` 的 `renderViewLoading / renderViewError` 改为直接 import `loading / error`
- 已完成：`scripts/verify-script-load.mjs` 反回归扫描扩展到 `zhimuStatus`，`banned = ["zhimuUiSemantics", "zhimuCollapsePanel", "zhimuStatus"]`。
- 已保留：`src/components/status-ui.js` 的 `normalizeError` 仍调用 `window.zhimuUserMessages?.friendlyApiError?.()`（下一个迁移目标），其它路径已不再读 `window.zhimuStatus`。
- 已完成：`scripts/ui-semantics.test.mjs` 删除 `zhimuStatus` shim，统一走真实 ES Module。
- 已验证：`npm run check:modules`、`npm run build`（103 modules, 1.47s）、`npm run test:play`（23/23）、`npm run test:host`（9/9）、`node --test scripts/ui-semantics.test.mjs`（3/3）、`node --test scripts/runtime-stores.test.mjs`（28/28）均通过。
- 已完成：`window.zhimuUserMessages` 在 `src/` 与 `app.js` 中的消费点和发布桥已清除，`src/utils/user-messages.js` 已删除底部 window 桥赋值块，改为纯 ES Module 导出 12 个符号（`API_ERROR_MESSAGES / RESTORE_SCOPE_OPTIONS / RULE_PREVIEW_STATUS / ASSET_KIND_TABS / ASSET_KIND_LABELS / friendlyApiError / formatCloudPanelError / overviewHeroTitle / assetKindLabel / rulePreviewStatusLabel / isQuotaExceededError / handleApiErrorToast`）；12 个消费者全部改为直接 import：
  - `src/components/emptyState.js`（`formatCloudPanelError`，2 处调用）
  - `src/components/service-outage.js`（`formatCloudPanelError`，1 处调用）
  - `src/components/status-ui.js`（`friendlyApiError`，`normalizeError` 跨桥依赖收口）
  - `src/views/account.js`（`handleApiErrorToast`，删除本地 fallback）
  - `src/views/assets.js`（`ASSET_KIND_TABS / assetKindLabel`，删除 2 行本地 fallback）
  - `src/views/archive.js`（`RESTORE_SCOPE_OPTIONS`，删除本地 fallback）
  - `src/views/director.js`（`rulePreviewStatusLabel`，删除本地 fallback）
  - `src/views/overview.js`（`overviewHeroTitle / formatCloudPanelError`，hero 区 2 处调用）
  - `src/views/writer.js`（`handleApiErrorToast`，协作邀请错误处理）
  - `src/runtime/auth-world.js`（`handleApiErrorToast / friendlyApiError`，邀请接受 + OAuth 错误 2 处调用）
  - `src/runtime/runtime-store.js`（`friendlyApiError`，`formatHostPlayersLoadError` 简化为单行）
  - `src/runtime/dependency-guard.js`（`requiredAppGlobals` 清空为 `[]`，不再要求 `zhimuUserMessages` 启动就绪）
- 已完成：`scripts/verify-script-load.mjs` 反回归扫描扩展到 `zhimuUserMessages`，`banned = ["zhimuUiSemantics", "zhimuCollapsePanel", "zhimuStatus", "zhimuUserMessages"]`。
- 已完成：`scripts/format-helpers.test.mjs` 与 `scripts/runtime-stores.test.mjs` 删除 `zhimuUserMessages` shim（迁移后子模块直接 import，不再读 window）。
- 已验证：`npm run check:modules`、`npm run build`（103 modules, 1.33s, index chunk -1.54 kB）、`npm run test:play`（23/23）、`npm run test:host`（9/9）、`node --test scripts/format-helpers.test.mjs scripts/runtime-stores.test.mjs scripts/ui-semantics.test.mjs`（37/37）均通过。
- 剩余：`zhimuFormat` / `zhimuModal` / `zhimuUi` / `zhimuUiSemantics` / `zhimuCollapsePanel` / `zhimuStatus` / `zhimuUserMessages` 已完成。L1-01 主应用 window 桥清理收尾，下一步聚焦 L1-03 生产门槛（CSP enforce、上传扫描、OTLP、告警 webhook）。

## A4 phase 5 shared layer

- 已完成：三端 Vite alias 配置。
- 已完成：`shared/api-error.js` 合并 play/host 错误格式化共性，保留端特定错误码和 host 的 403 文案策略。
- 已完成：`shared/sse.js` 合并主应用/play/host SSE block 解析、JSON 解析与 Last-Event-ID 游标持久化；connected / heartbeat / type 拆解等端特定行为仍留在调用方。
- 已完成：`shared/components/collapse.js` 合并主应用/host 折叠面板模板，并默认转义 id、title、subtitle。
- 已验证：`npm run check:modules`、`npm run build`、`npm run test:play`、`npm run test:host` 均通过。

## L1-02 key E2E

- 已完成：Chromium 关键 E2E suite 本地全绿，15 passed。
- 已修复：官方示例 fixture 会种入 `OFFICIAL_EXAMPLE_WORLD_ID` 对应的最小可体验世界。
- 已修复：CI 后端启动阶段同步接收官方示例、游客限流、社交冷却等 E2E 所需环境变量。
- 已修复：玩家入房 helper 会优先等待角色卡出现，不再在按钮 disabled / DOM 替换窗口里卡住。
- 已修复：`e2e/helpers/fixture.mjs` 删除重复 `joinRoomViaInviteUi` 定义，避免后定义覆盖带注释版本；`node --check` 已通过。
- 已调整：`waitForCloudReady` 增加 `window.zhimuState` 桥存在性快检；主持端 helper 不再读取 host 页面不存在的 `window.zhimuState`。
- 已调整：`playwright.config.js` 补充 `host/` 独立 webServer，并同步官方示例 world id 与单测超时。

## L1-03 production gates

- 已完成：本地非生产信任门槛检查通过，`http://127.0.0.1:4180/api/health/ready` 正常。
- 已完成：生产 `productionTrust` **7/7**（2026-07-03；迁移 46；`node scripts/verify-production-trust.mjs`）。
- 已完成：Cloudflare Ops Bridge（`https://ops.getzhimu.com`）承接告警、上传扫描、OTLP。
- 已完成：Railway 环境变量同步（CSP enforce、OTLP、告警、上传扫描、OPS/METRICS token）。

### L1-03 代码安全加固（已完成）

4 项 productionTrust 缺口全部是环境变量配置（代码已就绪），无需新增业务代码。但在审查中发现 3 个代码层面的安全加固点，已修复：

- 已完成：`backend/src/ops-auth.js` OPS token 与 METRICS token 比较改用 `crypto.timingSafeEqual` 常量时间比较，消除时序侧信道风险。
- 已完成：`backend/src/ops-auth.js` `requireMetricsToken` 在 `NODE_ENV=production` 且未配置 `METRICS_TOKEN` 时返回 503 拒绝（此前为放行），符合生产安全默认。非生产环境保持放行以不影响开发体验。
- 已完成：`backend/src/error-codes.js` 新增 `METRICS_NOT_CONFIGURED`（503）错误码。
- 已完成：`backend/src/startup-validation.js` 生产环境新增 `METRICS_TOKEN` 缺失警告，与 `OPS_API_TOKEN` 警告并列。
- 已验证：后端 `check:modules`（173 files）、`ops-metrics`（5/5，含 2 个新增测试）、`ops-audit + ops-catalog + beta2-ops + ops-health`（11/11）、前端 `check:modules` + `build`（103 modules, 952ms）均通过。

### productionTrust 7/7 部署清单（已配置）

| gate | 需要的环境变量 | 代码位置 |
|---|---|---|
| `csp` | `CSP_MODE=enforce` | `backend/src/security-headers.js` |
| `upload_scan` | `UPLOAD_SCAN_MODE=strict` + `UPLOAD_SCAN_WEBHOOK_URL` | `backend/src/upload-scan*.js` |
| `telemetry` | `OTEL_EXPORTER_OTLP_ENDPOINT` + `OTEL_ENABLED=true` | `backend/src/telemetry.js` |
| `alerts` | `ALERT_WEBHOOK_URL` | `backend/src/ops-alert-bridge.js` |

生产值已通过 `scripts/cloudflare-deploy-ops-worker.mjs` + `npm run railway:push-env` 写入。

### P1-04 线索审稿 API（已完成）

- 已完成：`backend/src/clue-audit.js` + `GET /api/worlds/:worldId/clue-audit`。
- 前端 `src/views/clues.js` 已有同源 UI；API 供聚合与 OPS 复用。

### P1-05 规则 debug trace（已完成）

- 已完成：`traceConditions` / `failedConditions` 接入 `previewRoomRules`。
- 已完成：主持端规则预览展示未满足条件明细。

### L1-04 备份恢复演练（已完成 2026-07-03）

- 已完成：`npm run db:verify-restore:managed` 对 Supabase 生产库通过（users/worlds/chapters/asset_files/auth_sessions 行数一致）。
- 记录：`docs/ops/BACKUP_DRILL_2026-07-03.md`；Runbook 已更新 `docs/ops/BACKUP.md`。
- 待补：Docker 可用时跑 `db:verify-restore:docker` 做全量 pg_dump 演练。

## P1-01 创作者制作总控台（L2-01）

对应路线图 `L2-01 | 创作者制作总控台 | 世界总览升级为制作状态、风险、下一步建议`，落点 `src/views/overview.js`。

- 已完成：`overview()` 读取 `cloudCreatorChecks / storageUsage / cloudHostStuckCount / cloudCheckpoints`，把后端 `creator-checks`、运行时异常和配额预警统一汇总成风险面板。
- 已完成：新增 `overviewRiskItem` 渲染函数，按 `level`（error/warning）输出图标、标题、详情与可点按钮（`data-action` 或 `data-go`）。
- 已完成：风险计算分三类：
  - 发布检查风险：`cloudCreatorChecks` 中的 error/warning 项全部进入风险列表。
  - 运行时风险：待确认事件 > 5、`cloudHostStuckCount > 0`、运行房未存档（`!cloudCheckpoints?.length`）。
  - 配额风险：`storageUsage.usedBytes / maxBytes > 0.8`，> 0.95 升级为 error。
- 已完成：发布就绪度指示器加入 `production-main` header：`readyForPlaytest`（无 error + 有角色/分幕/章节）、`readyForCatalog`（readyForPlaytest + 无 warning + 有运行房），用 `status-chip` 展示「尚未就绪 / 可内测测试 / 可申请公开库」。
- 已完成：新增独立 `<section class="risk-console">`，位于 `production-console` 与 `backend-console` 之间，包含风险计数（阻塞/警告/无风险）、风险列表与「运行发布检查」按钮。
- 已完成：`operationCards` 增加云存储配额卡片（仅当 `storageUsage.maxBytes` 存在时显示百分比与已用/总量）。
- 已完成：`nextActions` 重排优先级——阻塞项（`riskErrorCount > 0`）、卡关玩家干预、运行房存档三项风险驱动动作排在制作缺口之前；无风险且无待办时才建议进入运行控制台检查现场。
- 已完成：`styles.css` 新增 `.risk-console / .risk-summary / .risk-count / .risk-list / .risk-item / .risk-icon` 样式，error 用 `--clay`、warning 用 `--brass`，与现有 `production-item / next-action-row` 视觉语言一致；响应式断点同步覆盖 `.risk-item`。
- 已验证：`npm run check:modules`、`npm run build`（103 modules, 1.30s）、`npm run test:play`（23/23）、`npm run test:host`（9/9）、`npm run test:ui-semantics`（3/3）、`npm run test:runtime-stores`（28/28）均通过。

## P1-02 主持运行控制台（L2-02）

对应路线图 `L2-02 | 主持端运行控制台 | 首页聚合玩家、待确认、线索、日志、风险 | 主持人开场前 30 秒内掌握房间状态`，落点 `src/views/director.js` + `host/src/views/console.js`。

### 主应用 director.js

- 已完成：新增 `directorRiskItem` 渲染函数（error/warning 两级，含图标、标题、详情与可点按钮）。
- 已完成：风险计算分 5 项：玩家加载失败（error）、实时推送未连接（warning）、待确认事件积压 > 5（warning）、卡关玩家 > 0（warning）、无启用规则（warning）。
- 已完成：开场就绪度指示器加入 RUN CONTROL header：`roomReady = !hostPlayersError && roomEventsConnected && joinedCount > 0`，用 `status-chip` 展示「运行就绪 / 等待玩家入房 / 连接中 / 初始化中」。
- 已完成：新增 `<section class="risk-console">` 插入在 `director-console` 与 `stats-grid` 之间，复用 P1-01 的 `.risk-console` CSS。
- 已保留：现有 `priorityActions` 主持优先动作列表（已有风险驱动项）继续工作。

### 独立主持端 host/src/views/console.js

- 已完成：`renderConsole()` 新增风险计算（5 项，与主应用一致）+ 就绪度计算 + 主持优先动作列表。
- 已完成：stats-grid 末尾增加「房间状态」就绪度 chip 卡片。
- 已完成：新增 `hostRiskPanel` 插入在 stats-grid 与 noPlayerProgressHint 之间，包含风险计数、风险列表与主持优先动作。
- 已完成：`host/src/styles.css` 新增 `.risk-console / .risk-summary / .risk-count / .risk-list / .risk-item / .risk-icon / .host-priority-list / .host-priority-action` 样式，视觉语言与主应用一致。
- 已验证：`npm run test:host`（9/9）含 `standalone console keeps the full host monitoring action surface` 通过。

## P1-03 玩家下一步行动（L2-03）

对应路线图 `L2-03 | 玩家下一步行动 | 玩家端首页明确"读什么、查什么、等什么" | 玩家不依赖主持口头解释即可继续`，落点 `src/views/player.js` + `play/src/views/game.js`。

### 主应用 player.js

- 已完成：`playerNextStep()` 从单一动作扩展为「主推动作 + 读什么/查什么/等什么 三段清单」。
- 已完成：「读什么」聚合未读分幕（slice 3）、未读自有线索（slice 2）、未读共享线索（slice 2），每项带 `data-action` 按钮直接跳转。
- 已完成：「查什么」聚合可调查点（`!investigated && hasRequiredItem`，slice 3）、缺道具调查点（slice 2）。
- 已完成：「等什么」聚合主持人待确认（`pendingCount && !waitingForYou`）、等待场景解锁、无紧急待办。
- 已完成：`styles.css` 新增 `.player-next-lists / .next-list / .next-list-item` 样式（三栏 grid，响应式降为单栏）。

### 独立玩家端 play/src/views/game.js

- 已完成：`renderGameHome()` 的 `.next-action` 卡片替换为 `renderPlayerActionsHub()` 函数。
- 已完成：主推动作按优先级推导：等待主持确认 → 继续阅读 → 去调查 → 读线索 → 自由讨论。
- 已完成：三段清单「📖 读什么 / 🔍 查什么 / ⏳ 等什么」，数据源全部来自 `state.home` / `state.exploration` / `state.currentGame`，无需新增后端接口。
- 已完成：`play/src/styles.css` 更新 `.next-action` 为 flex column 布局，新增 `.next-action-primary / .next-action-lists / .action-col / .action-col-label / .action-list-item` 样式（响应式 720px 降为单栏）。
- 已验证：`npm run test:play`（23/23）含 `render layer escapes user content` 通过。

### 统一验证

- 已验证：`npm run check:modules`、`npm run build`（103 modules, 952ms）、`npm run test:play`（23/23）、`npm run test:host`（9/9）、`npm run test:ui-semantics`（3/3）、`npm run test:runtime-stores`（28/28）均通过。

## P1 公开 Beta 自助闭环 — 反馈入口（已完成）

对应路线图 `P1 | 公开 Beta 自助闭环 | 应用内反馈入口、错误/故障入口`，落点 `backend/src/feedback.js` + `src/components/feedback-button.js`。

### 后端反馈系统

- 已完成：`backend/migrations/046_feedback.sql` 新增 `feedback` 表（id / user_id / kind / subject / body / page_url / user_agent / status / created_at / updated_at），含 kind 与 status CHECK 约束 + status/kind 索引。
- 已完成：`backend/src/feedback.js` 反馈业务逻辑：
  - `submitFeedback(body, actorId)` — 校验 kind（feedback/bug/feature）、subject（必填，≤200）、body（必填，≤4000），自动捕获 pageUrl/userAgent，INSERT 后返回记录。
  - `listFeedback({ status, kind, limit, offset })` — 分页查询，LEFT JOIN users 获取用户信息，返回 `{ items, total }`。
  - `getFeedbackStats()` — 按 status 分组计数，供 OPS 控制台使用。
  - `updateFeedbackStatus(id, status)` — 更新状态（new/seen/resolved），未找到抛 `FEEDBACK_NOT_FOUND`。
- 已完成：`backend/src/error-codes.js` 新增 5 个反馈错误码（`FEEDBACK_KIND_INVALID / FEEDBACK_SUBJECT_REQUIRED / FEEDBACK_BODY_REQUIRED / FEEDBACK_STATUS_INVALID / FEEDBACK_NOT_FOUND`）。
- 已完成：`backend/src/routes/feedback-routes.js` 公开反馈提交端点 `POST /api/feedback`（无需认证，可选关联 actorId）。
- 已完成：`backend/src/routes/ops-routes.js` 新增 3 个 OPS 端点（均由 preHandler `requireOpsToken` 守卫）：
  - `GET /api/ops/feedback` — 分页查询，支持 status/kind 筛选。
  - `GET /api/ops/feedback/stats` — 按 status 分组计数。
  - `PATCH /api/ops/feedback/:id` — 更新反馈状态。
- 已完成：`backend/src/routes.js` 注册 `registerFeedbackRoutes`。
- 已完成：`backend/test/feedback.test.js` 新增 9 个测试覆盖公开提交、输入校验、默认值、OPS 鉴权、分页查询、统计、状态更新、404。
- 已验证：`node --test test/feedback.test.js`（9/9）、`ops-metrics + ops-audit + ops-catalog + ops-health`（14/14）均通过。

### 前端反馈入口

- 已完成：`src/components/feedback-button.js` 全局反馈按钮 + 弹窗表单：
  - `openFeedbackForm(prefillKind, prefillSubject, prefillBody)` — 构建 modal 表单（类型 select + 主题 input + 详情 textarea），提交调用 `request("/feedback", { method: "POST", body })`，成功 `showToast` + `closeModal`。
  - `mountFeedbackButton()` — 在 `document.body` 挂载浮动反馈按钮，点击打开表单。
  - 自动捕获 `pageUrl`（`window.location.href`）和 `userAgent`（`navigator.userAgent`）随表单提交。
- 已完成：`app.js` 集成 `mountFeedbackButton()`（import + 在 `initEvents` 之后、`render()` 之前调用）。
- 已完成：`styles.css` 新增 `.feedback-fab`（浮动圆角按钮，右下角 fixed，含 hover/focus 状态与 720px 响应式）和 `.feedback-modal`（表单 form-group/field/textarea/modal-actions 样式）。

### OPS 处理视图

- 已完成：`src/api/ops.js` 新增 `getOpsFeedback / getOpsFeedbackStats / updateOpsFeedbackStatus`，主应用 API aggregator 同步导出。
- 已完成：`src/views/ops.js` 运营控制台新增「用户反馈」面板，展示总数、待看数、反馈类型、提交用户、提交时间、正文和页面 URL。
- 已完成：`src/runtime/actions-ops.js` 新增 `ops-feedback-status`，支持在 OPS 页面将反馈按 `new → seen → resolved` 标记处理，并刷新列表。

### 错误/故障上报入口

- 已完成：`src/components/toast.js` 新增 `showToastWithAction(text, { actionLabel, onAction, duration })` — 支持带操作按钮的 toast（如"上报故障"），通过 callback 解耦避免与 feedback-button.js 循环依赖；`showToast` 与 `showToastWithAction` 共享单例 toast 计时器。
- 已完成：`src/runtime/actions.js` 新增 `report-issue` data-action 处理，读取 `data-report-subject` / `data-report-body` 后调用 `openFeedbackForm("bug", subject, body)`。
- 已完成：`src/components/service-outage.js` 503 故障页新增「上报故障」按钮（`data-action="report-issue"`），预填主题"云端连接故障"与详情（错误信息 + 页面 URL + 用户补充提示），使用 `shared/security.js` 的 `escapeHtml` 安全转义 data 属性。

### 统一验证

- 已验证：`npm run check:modules`（all passed，含 app.js feedback-button import）、`npm run build`（104 modules, 980ms）、`npm run test:play`（23/23）、`npm run test:host`（9/9）、`npm run test:ui-semantics`（3/3）、`npm run test:runtime-stores`（28/28）均通过。
- 已验证：后端 `feedback.test.js`（9/9）、`ops-metrics + ops-audit + ops-catalog + ops-health`（14/14）均通过。
