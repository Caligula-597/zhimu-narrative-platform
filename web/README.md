# Legacy dual-service Web deploy (deprecated)

Production uses **single-service fullstack**: `deploy/Dockerfile.fullstack`.

See [docs/PLATFORM_MAP_ZH.md](../docs/PLATFORM_MAP_ZH.md) and [docs/ops/DEPLOY.md](../docs/ops/DEPLOY.md).

Do not create a separate Railway `web` service unless you know you need the old layout.
