---
description: 改动范围检测通过后自动 commit 并 push，无需再向用户确认
alwaysApply: true
---

# 自动提交与推送

完成代码修改后，**默认每次**自动 commit + push。**不要询问**用户是否提交。

## 1. 仅检测改动部分（禁止默认全量）

**不要**默认跑 `backend npm test` 全套件、`verify:full`、E2E，或需本地服务已起的 HTTP smoke。

统一用：

```bash
npm run verify:changed
```

该脚本根据 `git diff` 变更路径自动选择最小检测，例如：

| 改动 | 检测 |
|------|------|
| 任意 `.js/.mjs` | `node --check` 仅变更文件 |
| `backend/src/foo.js` | 若存在 `backend/test/foo.test.js` 则只跑该文件 |
| `backend/src/script-bundle*` | `script-bundle*.test.js` |
| `backend/src/pdf-document*`、`document-*` | `pdf-document.test.js` |
| `backend/src/routes/schemas*` | `npm run check:schemas` |
| `src/`、`app.js`、`index.html`、`frontend/` | 若动到入口/视图/组件 → `npm run check:modules` |
| `src/views/pipeline-*` | `npm run test:pipeline-session` |
| 仅 `docs/`、`.cursor/rules/`、`.md` | 跳过测试（仍检查是否含密钥路径） |

若需全量验证，仅当用户明确要求或发布前手动执行。

## 2. 通过后立即 commit + push

1. `git status` / `git diff` — 不得含 `.env`、密钥、token、`chi_sim.traineddata` 等
2. `git add` 仅相关文件
3. `git commit` — 1～2 句，说明 why
4. `npm run git:push` — 经 Clash 推送（见 `git-clash-proxy.mdc`；**不要** force push `main`/`master`；**不要** `git config --global http.proxy`）

## 3. 禁止自动提交

- `npm run verify:changed` 失败
- 改动含疑似密钥或 `.env`
- 用户在本轮对话明确说「先别提交」或「不要 push」

## 4. 回复用户

完成后简短说明：commit hash、分支、**实际跑了哪些 verify-changed 项**。**不要再问**「要不要提交」。
