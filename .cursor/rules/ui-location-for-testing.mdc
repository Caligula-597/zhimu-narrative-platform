---
description: 新功能交付时必须标注 UI 位置，方便创作者验收测试
alwaysApply: true
---

# UI 位置标注（验收必备）

每次新增或改动**用户可见功能**时，在回复末尾必须附带 **「验收位置」** 小节，用中文写清楚从哪进、点哪、看到什么。

## 必填格式

```markdown
## 验收位置

| 项目 | 说明 |
|------|------|
| 视图 | 例如：侧栏 → 某世界 → 剧本杀创作中心 |
| 区域 | 例如：Hero 顶栏 / 左栏导航 / 弹窗 Tab / 底栏按钮 |
| 控件 | 按钮文案、Tab 名、字段标签（与界面一致） |
| 操作 | 1～3 步：如何打开、如何触发、预期结果 |
| 文件 | 主要实现文件（可选，便于对照） |
```

## 层级命名约定

- **视图**：应用一级导航（世界列表、创作中心、房间等）
- **区域**：该视图内的区块（Hero、卡片、弹窗、侧栏、底栏）
- **控件**：用户可点的按钮/链接/Tab，用界面上的**原文**
- 若有弹窗，写清**弹窗标题**和**位置条**（breadcrumb）文字

## 示例

```markdown
## 验收位置

| 项目 | 说明 |
|------|------|
| 视图 | 进入任意世界 → 剧本杀创作中心 |
| 区域 | Hero 顶栏右侧按钮组 |
| 控件 | 主按钮「AI 悬疑创作」 |
| 操作 | 点击后打开标题为「AI 悬疑创作」的全屏弹窗；默认 Tab「分步参与」；左栏为 ①～⑦ 层级 |
| 文件 | `src/views/writer.js`（入口）、`src/views/pipeline-wizard.js`（弹窗） |
```

## 何时必须写

- 新按钮、新 Tab、新弹窗、新表单字段
- 入口合并、移动、重命名
- 仅改后端/API 但影响界面文案或流程时

## 何时可省略

- 纯重构、无 UI 变化
- 仅修 bug 且行为与位置不变（可写「位置不变：…」一句带过）
