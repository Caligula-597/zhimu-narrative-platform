织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料
生成时间：2026-08-08T03:03:27.593Z
纳入文件数：1011
源代码总行数：140751
纳入目录：backend/src、src、play/src、host/src、shared、backend/migrations
前段新增功能证据：shared/mechanism-design.js、shared/mechanism-interactions.js、src/views/creator-mechanism-workbench.js、src/views/creator-mechanism-workbench.css、src/runtime/actions-creator-cockpit.js、backend/src/room-mechanism-runtime-service.js
后段新增功能证据：backend/migrations/107_room_mechanism_decision_submissions.sql、backend/migrations/108_room_mechanism_round_clock.sql、backend/src/room-mechanism-submission-service.js、backend/src/routes/player-progress-routes.js、play/src/runtime/game-action-controller.js、play/src/views/game-home-views.js、host/src/runtime/host-mechanism-controller.js、host/src/views/host-mechanism-workspace.js、backend/src/routes/host-mechanism-runtime-routes.js
连续性说明：前 30 页与后 30 页分别从确定性文件序列连续截取，每页 50 行，页际不跳行。
说明：本文件为软著普通交存源程序摘录稿，包含前 30 页和后 30 页，每页 50 行。

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 1 页 =====

01: // ===== 文件：shared/mechanism-design.js =====
02: import {
03:   DEFAULT_MECHANISM_INTERACTION_KIND,
04:   isMechanismInteractionKind,
05:   mechanismInteractionCard,
06: } from "./mechanism-interactions.js";
07:
08: export const MECHANISM_DESIGN_VERSION = 1;
09: export const MECHANISM_DESIGN_STATUSES = Object.freeze(["draft", "confirmed"]);
10:
11: export const MECHANISM_DESIGN_QUESTIONS = Object.freeze([
12:   Object.freeze({
13:     key: "recurringAction",
14:     label: "玩家反复执行的动作",
15:     prompt: "写具体动作，例如调取一份档案、分配氧气或提出仲裁动议。",
16:   }),
17:   Object.freeze({
18:     key: "conflictReason",
19:     label: "为什么不能全部合作",
20:     prompt: "说明目标、权限、信息或利益为什么互相冲突。",
21:   }),
22:   Object.freeze({
23:     key: "limitedResource",
24:     label: "题材内有限资源",
25:     prompt: "写世界中真实存在的名额、时间、权限、物资或机会。",
26:   }),
27:   Object.freeze({
28:     key: "immediateFeedback",
29:     label: "选择后的即时反馈",
30:     prompt: "玩家行动后立刻看见什么变化。",
31:   }),
32:   Object.freeze({
33:     key: "failureAdvance",
34:     label: "失败如何继续推进",
35:     prompt: "失败不能停局；写明损失、替代路径或新的危险。",
36:   }),
37:   Object.freeze({
38:     key: "genreSpecificity",
39:     label: "为什么只适合这个题材",
40:     prompt: "说明它怎样利用本作独有的职业、制度、空间或世界规则。",
41:   }),
42:   Object.freeze({
43:     key: "endingCausality",
44:     label: "早期选择怎样影响结局",
45:     prompt: "指出前几轮选择会改变的路线、资源或人物关系。",
46:   }),
47: ]);
48:
49: function record(value) {
50:   return value && typeof value === "object" && !Array.isArray(value)

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 2 页 =====

01:     ? value
02:     : {};
03: }
04:
05: function cleanText(value, maximum = 2400) {
06:   return String(value ?? "")
07:     .trim()
08:     .slice(0, maximum);
09: }
10:
11: export function normalizeMechanismDesign(value = {}) {
12:   const source = record(value);
13:   const interactionKind = isMechanismInteractionKind(source.interactionKind)
14:     ? String(source.interactionKind)
15:     : DEFAULT_MECHANISM_INTERACTION_KIND;
16:   const status = MECHANISM_DESIGN_STATUSES.includes(source.status)
17:     ? source.status
18:     : "draft";
19:   return {
20:     version: MECHANISM_DESIGN_VERSION,
21:     interactionKind,
22:     title: cleanText(source.title, 160),
23:     summary: cleanText(source.summary, 1200),
24:     recurringAction: cleanText(source.recurringAction),
25:     conflictReason: cleanText(source.conflictReason),
26:     limitedResource: cleanText(source.limitedResource),
27:     immediateFeedback: cleanText(source.immediateFeedback),
28:     failureAdvance: cleanText(source.failureAdvance),
29:     genreSpecificity: cleanText(source.genreSpecificity),
30:     endingCausality: cleanText(source.endingCausality),
31:     authorNotes: cleanText(source.authorNotes, 4000),
32:     status,
33:     updatedAt: cleanText(source.updatedAt, 80),
34:   };
35: }
36:
37: export function mechanismDesignCoverage(value = {}) {
38:   const design = normalizeMechanismDesign(value);
39:   const filledKeys = MECHANISM_DESIGN_QUESTIONS.map(
40:     (question) => question.key,
41:   ).filter((key) => Boolean(design[key]));
42:   return {
43:     filled: filledKeys.length,
44:     total: MECHANISM_DESIGN_QUESTIONS.length,
45:     score: Math.round(
46:       (filledKeys.length / MECHANISM_DESIGN_QUESTIONS.length) * 100,
47:     ),
48:     complete: filledKeys.length === MECHANISM_DESIGN_QUESTIONS.length,
49:     missingKeys: MECHANISM_DESIGN_QUESTIONS.map(
50:       (question) => question.key,

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 3 页 =====

01:     ).filter((key) => !filledKeys.includes(key)),
02:   };
03: }
04:
05: export function mechanismDesignHasContent(value = {}) {
06:   const design = normalizeMechanismDesign(value);
07:   return Boolean(
08:     design.title ||
09:       design.summary ||
10:       design.authorNotes ||
11:       MECHANISM_DESIGN_QUESTIONS.some((question) => design[question.key]),
12:   );
13: }
14:
15: export function validateMechanismDesignConfirmation(value = {}) {
16:   const design = normalizeMechanismDesign(value);
17:   const issues = [];
18:   if (!design.title) {
19:     issues.push({
20:       key: "title",
21:       message: "请填写机制名称，主持端和玩家端都会用它识别本机制。",
22:     });
23:   }
24:   if (!design.summary) {
25:     issues.push({
26:       key: "summary",
27:       message: "请填写一句话概述，说明玩家如何行动以及选择会改变什么。",
28:     });
29:   }
30:   for (const question of MECHANISM_DESIGN_QUESTIONS) {
31:     if (!design[question.key]) {
32:       issues.push({
33:         key: question.key,
34:         message: `请回答「${question.label}」。`,
35:       });
36:     }
37:   }
38:   return {
39:     valid: issues.length === 0,
40:     issues,
41:     design,
42:     coverage: mechanismDesignCoverage(design),
43:   };
44: }
45:
46: export function formatMechanismDesignForPrompt(value = {}) {
47:   const design = normalizeMechanismDesign(value);
48:   const coverage = mechanismDesignCoverage(design);
49:   if (!coverage.filled && !design.title && !design.summary) return [];
50:   const card = mechanismInteractionCard(design.interactionKind);

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 4 页 =====

01:   const rows = [
02:     `设计状态：${design.status === "confirmed" ? "作者已确认" : "作者草稿，不得擅自补成既定事实"}`,
03:     `线上表现：${card.label}`,
04:     design.title ? `机制名称：${design.title}` : "",
05:     design.summary ? `机制概述：${design.summary}` : "",
06:     ...MECHANISM_DESIGN_QUESTIONS.map((question) =>
07:       design[question.key] ? `${question.label}：${design[question.key]}` : "",
08:     ),
09:     design.authorNotes ? `作者备注：${design.authorNotes}` : "",
10:   ];
11:   return rows.filter(Boolean);
12: }
13:
14: // ===== 文件结束：shared/mechanism-design.js =====
15:
16: // ===== 文件：shared/mechanism-interactions.js =====
17: /**
18:  * Cross-surface presentation contract for authored mechanism decisions.
19:  *
20:  * The runtime remains authoritative and deterministic.  This module only
21:  * describes how the same decision should be explained to an author, operated
22:  * by a host and presented to players.  Adding a new kind must therefore start
23:  * here instead of growing three unrelated UI switches.
24:  */
25:
26: export const DEFAULT_MECHANISM_INTERACTION_KIND = "group_choice";
27: export const MECHANISM_INTERACTION_RESOLUTION = "host_confirmed";
28: export const MECHANISM_INTERACTION_SUBMISSION_MODE = "advisory_choice";
29: export const MECHANISM_INTERACTION_SUBMISSION_MODES = Object.freeze([
30:   "advisory_choice",
31:   "private_choice",
32: ]);
33:
34: export const MECHANISM_INTERACTION_CARDS = Object.freeze([
35:   Object.freeze({
36:     key: "group_choice",
37:     label: "公开抉择",
38:     shortLabel: "讨论",
39:     theme: "discussion",
40:     authorPrompt: "全桌公开讨论并形成唯一选择。",
41:     playerInstruction: "讨论方案后，把全桌决定交给主持人。",
42:     hostInstruction: "确认全桌决定后结算，不替玩家选择。",
43:   }),
44:   Object.freeze({
45:     key: "resource_tradeoff",
46:     label: "资源取舍",
47:     shortLabel: "分配",
48:     theme: "resource",
49:     authorPrompt: "用有限资源交换区域、权限、时间或安全。",
50:     playerInstruction: "比较每个方案会消耗和保住什么。",

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 5 页 =====

01:     hostInstruction: "核对剩余资源；不足时执行失败推进。",
02:   }),
03:   Object.freeze({
04:     key: "evidence_selection",
05:     label: "证据质证",
06:     shortLabel: "质证",
07:     theme: "evidence",
08:     authorPrompt: "选择认可、公开、封存或组合哪组材料。",
09:     playerInstruction: "核对材料边界，再选择证据方案。",
10:     hostInstruction: "只按已取得且有效的证据结算。",
11:   }),
12:   Object.freeze({
13:     key: "sequence_reconstruction",
14:     label: "顺序重建",
15:     shortLabel: "排序",
16:     theme: "sequence",
17:     authorPrompt: "重建事件、档案、语言或行动顺序。",
18:     playerInstruction: "比较各套顺序，再提交完整方案。",
19:     hostInstruction: "结算整套方案，不逐条发放答案。",
20:   }),
21:   Object.freeze({
22:     key: "timed_crisis",
23:     label: "限时危机",
24:     shortLabel: "限时",
25:     theme: "timed",
26:     authorPrompt: "时限内行动，超时也产生可继续的结果。",
27:     playerInstruction: "在时限内行动；到期执行默认损失。",
28:     hostInstruction: "按作品时钟结算，不临时延长。",
29:   }),
30:   Object.freeze({
31:     key: "role_commitment",
32:     label: "角色承诺",
33:     shortLabel: "承诺",
34:     theme: "role",
35:     authorPrompt: "角色承诺会改变自身权限、关系或路线。",
36:     playerInstruction: "按角色目标选择愿意承担的立场。",
37:     hostInstruction: "确认承诺主体并记录后续影响。",
38:   }),
39: ]);
40:
41: const CARD_BY_KEY = new Map(
42:   MECHANISM_INTERACTION_CARDS.map((card) => [card.key, card]),
43: );
44:
45: function record(value) {
46:   return value && typeof value === "object" && !Array.isArray(value)
47:     ? value
48:     : {};
49: }
50:

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 6 页 =====

01: function cleanText(value, maxLength = 600) {
02:   return String(value ?? "")
03:     .trim()
04:     .slice(0, maxLength);
05: }
06:
07: function cleanKey(value) {
08:   return cleanText(value, 120);
09: }
10:
11: function boundedInteger(value, fallback = 0, minimum = 0, maximum = 7200) {
12:   const parsed = Number.parseInt(value, 10);
13:   if (!Number.isFinite(parsed)) return fallback;
14:   return Math.max(minimum, Math.min(maximum, parsed));
15: }
16:
17: function submissionModeFor(card) {
18:   return card.key === "role_commitment"
19:     ? "private_choice"
20:     : MECHANISM_INTERACTION_SUBMISSION_MODE;
21: }
22:
23: export function mechanismInteractionCard(
24:   kind = DEFAULT_MECHANISM_INTERACTION_KIND,
25: ) {
26:   return (
27:     CARD_BY_KEY.get(kind) || CARD_BY_KEY.get(DEFAULT_MECHANISM_INTERACTION_KIND)
28:   );
29: }
30:
31: export function isMechanismInteractionKind(kind) {
32:   return CARD_BY_KEY.has(String(kind ?? "").trim());
33: }
34:
35: export function normalizeMechanismInteraction(value = {}) {
36:   const source = record(value);
37:   const card = mechanismInteractionCard(cleanKey(source.kind));
38:   return {
39:     kind: card.key,
40:     resolutionMode: MECHANISM_INTERACTION_RESOLUTION,
41:     submissionMode: submissionModeFor(card),
42:     label: cleanText(source.label, 120) || card.label,
43:     playerInstruction:
44:       cleanText(source.playerInstruction) || card.playerInstruction,
45:     hostInstruction: cleanText(source.hostInstruction) || card.hostInstruction,
46:     deadlineSeconds:
47:       card.key === "timed_crisis"
48:         ? boundedInteger(source.deadlineSeconds, 0, 0, 7200)
49:         : 0,
50:     defaultOptionKey:

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 7 页 =====

01:       card.key === "timed_crisis" ? cleanKey(source.defaultOptionKey) : "",
02:     resourceKey:
03:       card.key === "resource_tradeoff" ? cleanKey(source.resourceKey) : "",
04:   };
05: }
06:
07: export function normalizeMechanismOptionPresentation(value = {}) {
08:   const source = record(value);
09:   return {
10:     eyebrow: cleanText(source.eyebrow, 80),
11:     publicPreview: cleanText(source.publicPreview, 500),
12:     costLabel: cleanText(source.costLabel, 160),
13:     riskLabel: cleanText(source.riskLabel, 160),
14:     sequenceLabel: cleanText(source.sequenceLabel, 80),
15:   };
16: }
17:
18: export function publicMechanismInteraction(value = {}) {
19:   const interaction = normalizeMechanismInteraction(value);
20:   return {
21:     kind: interaction.kind,
22:     resolutionMode: interaction.resolutionMode,
23:     submissionMode: interaction.submissionMode,
24:     label: interaction.label,
25:     playerInstruction: interaction.playerInstruction,
26:     deadlineSeconds: interaction.deadlineSeconds,
27:     defaultOptionKey: interaction.defaultOptionKey,
28:   };
29: }
30:
31: // ===== 文件结束：shared/mechanism-interactions.js =====
32:
33: // ===== 文件：src/views/creator-mechanism-workbench.js =====
34: import "./creator-mechanism-workbench.css";
35: import { escapeHtml } from "../utils/format.js";
36: import { setHtml } from "../../shared/safe-dom.js";
37: import {
38:   MECHANISM_DESIGN_QUESTIONS,
39:   mechanismDesignCoverage,
40:   normalizeMechanismDesign,
41:   validateMechanismDesignConfirmation,
42: } from "../../shared/mechanism-design.js";
43: import {
44:   MECHANISM_INTERACTION_CARDS,
45:   mechanismInteractionCard,
46: } from "../../shared/mechanism-interactions.js";
47:
48: function fieldHtml(question, value, invalidKeys) {
49:   const invalid = invalidKeys.has(question.key);
50:   return `<label class="mechanism-design-field ${invalid ? "is-invalid" : ""}">

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 8 页 =====

01:     <span>${escapeHtml(question.label)}</span>
02:     <small>${escapeHtml(question.prompt)}</small>
03:     <textarea rows="3" maxlength="2400" data-mechanism-design-field="${escapeHtml(question.key)}" ari ...
04:   </label>`;
05: }
06:
07: function renderFrame(
08:   root,
09:   design,
10:   saving = false,
11:   discardArmed = false,
12:   validationIssues = [],
13:   saveError = "",
14: ) {
15:   const coverage = mechanismDesignCoverage(design);
16:   const activeCard = mechanismInteractionCard(design.interactionKind);
17:   const invalidKeys = new Set(validationIssues.map((issue) => issue.key));
18:   const validationMessage = validationIssues.length
19:     ? `<section class="mechanism-validation-message" role="alert" aria-label="机制设计确认校验失败"><strong>还不能 ...
20:     : saveError
21:       ? `<section class="mechanism-validation-message is-save-error" role="alert"><strong>机制设计保存失败</s ...
22:       : "";
23:   setHtml(
24:     root,
25:     `<section class="creator-mechanism-workbench" data-workspace-editor aria-label="机制设计工作台" aria-bus ...
26:   <div class="mechanism-workbench-head">
27:     <div><p>MECHANISM WORKBENCH</p><h2>机制设计工作台</h2><span>先把玩家反复做什么说清楚，再交给AI扩写章节。</span></div>
28:     <div class="mechanism-coverage"><strong>${coverage.filled}/${coverage.total}</strong><small>机制七问< ...
29:   </div>
30:   <section class="mechanism-design-section">
31:     <div class="mechanism-design-section-head"><div><span>01</span><h3>选择线上表现形式</h3></div><p>它决定玩家端如何 ...
32:     <div class="mechanism-design-card-grid">${MECHANISM_INTERACTION_CARDS.map(
33:       (card) => `
34:       <button type="button" class="mechanism-design-card ${card.key === design.interactionKind ? "act ...
35:         <span>${escapeHtml(card.shortLabel)}</span><strong>${escapeHtml(card.label)}</strong><p>${esc ...
36:       </button>`,
37:     ).join("")}</div>
38:     <div class="mechanism-cross-surface-preview">
39:       <article><span>玩家端</span><p>${escapeHtml(activeCard.playerInstruction)}</p></article>
40:       <article><span>主持端</span><p>${escapeHtml(activeCard.hostInstruction)}</p></article>
41:     </div>
42:   </section>
43:   <section class="mechanism-design-section">
44:     <div class="mechanism-design-section-head"><div><span>02</span><h3>回答机制七问</h3></div><p>写世界内行为，不写状 ...
45:     <div class="mechanism-design-basics">
46:       <label class="${invalidKeys.has("title") ? "is-invalid" : ""}"><span>机制名称</span><input data-mec ...
47:       <label class="${invalidKeys.has("summary") ? "is-invalid" : ""}"><span>一句话概述</span><textarea da ...
48:     </div>
49:     <div class="mechanism-design-question-grid">${MECHANISM_DESIGN_QUESTIONS.map((question) => fieldH ...
50:     <label class="mechanism-design-field wide"><span>补充给AI与主持人的作者备注</span><textarea rows="3" maxlengt ...

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 9 页 =====

01:   </section>
02:   ${validationMessage}
03:   <div class="mechanism-workbench-actions">
04:     <button type="button" class="secondary-btn" data-mechanism-close>${discardArmed ? "再次点击放弃修改" : "返 ...
05:     <div><span>${discardArmed ? "当前有未保存修改；再次返回才会放弃。" : design.status === "confirmed" ? "当前版本已确认为作者设定" ...
06:       <button type="button" class="secondary-btn" data-mechanism-save="draft" ${saving ? "disabled" : ...
07:       <button type="button" class="primary-btn" data-mechanism-save="confirmed" ${saving ? "disabled" ...
08:     </div>
09:   </div>
10: </section>`,
11:   );
12: }
13:
14: function readDesign(root, current) {
15:   const next = { ...current };
16:   root.querySelectorAll("[data-mechanism-design-field]").forEach((field) => {
17:     next[field.dataset.mechanismDesignField] = field.value;
18:   });
19:   return normalizeMechanismDesign(next);
20: }
21:
22: export function openCreatorMechanismWorkbench({
23:   root,
24:   value,
25:   onSave,
26:   onClose,
27: }) {
28:   if (!root) return false;
29:   let design = normalizeMechanismDesign(value);
30:   let saving = false;
31:   let dirty = false;
32:   let discardArmed = false;
33:   let validationIssues = [];
34:   let saveError = "";
35:
36:   function bind() {
37:     root
38:       .querySelector("[data-mechanism-close]")
39:       ?.addEventListener("click", () => {
40:         if (saving) return;
41:         if (dirty && !discardArmed) {
42:           discardArmed = true;
43:           render();
44:           return;
45:         }
46:         onClose?.();
47:       });
48:     root.querySelectorAll("[data-mechanism-kind]").forEach((button) => {
49:       button.addEventListener("click", () => {
50:         design = normalizeMechanismDesign({

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 10 页 =====

01:           ...readDesign(root, design),
02:           interactionKind: button.dataset.mechanismKind,
03:         });
04:         dirty = true;
05:         discardArmed = false;
06:         render();
07:       });
08:     });
09:     root
10:       .querySelector("[data-workspace-editor]")
11:       ?.addEventListener("input", () => {
12:         dirty = true;
13:         discardArmed = false;
14:         validationIssues = [];
15:         saveError = "";
16:       });
17:     root.querySelectorAll("[data-mechanism-save]").forEach((button) => {
18:       button.addEventListener("click", async () => {
19:         if (saving) return;
20:         const requestedStatus = button.dataset.mechanismSave;
21:         design = normalizeMechanismDesign({
22:           ...readDesign(root, design),
23:           status: requestedStatus,
24:           updatedAt: new Date().toISOString(),
25:         });
26:         if (requestedStatus === "confirmed") {
27:           const validation = validateMechanismDesignConfirmation(design);
28:           if (!validation.valid) {
29:             validationIssues = validation.issues;
30:             saveError = "";
31:             render();
32:             root
33:               .querySelector(
34:                 `[data-mechanism-design-field="${validationIssues[0].key}"]`,
35:               )
36:               ?.focus();
37:             return;
38:           }
39:         }
40:         validationIssues = [];
41:         saveError = "";
42:         saving = true;
43:         render();
44:         try {
45:           await onSave(design);
46:           dirty = false;
47:           discardArmed = false;
48:         } catch (error) {
49:           dirty = true;
50:           saveError = error?.message || "请检查网络连接后重试。";

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 11 页 =====

01:         } finally {
02:           saving = false;
03:           if (root.isConnected) render();
04:         }
05:       });
06:     });
07:   }
08:
09:   function render() {
10:     renderFrame(
11:       root,
12:       design,
13:       saving,
14:       discardArmed,
15:       validationIssues,
16:       saveError,
17:     );
18:     bind();
19:   }
20:
21:   render();
22:   return true;
23: }
24:
25: export async function openCurrentCreatorMechanismWorkbench() {
26:   const [
27:     zhimuApi,
28:     { worldStore },
29:     { showToast },
30:     { callView },
31:     { callRuntime },
32:   ] = await Promise.all([
33:     import("../api/index.js"),
34:     import("../state/index.js"),
35:     import("../components/toast.js"),
36:     import("../runtime/view-registry.js"),
37:     import("../runtime/runtime-facade.js"),
38:   ]);
39:   const worldId = zhimuApi.context.worldId;
40:   const root = document.querySelector(".creator-cockpit .cockpit-core-canvas");
41:   if (!root) return showToast("机制工作台暂时无法打开，请刷新驾驶舱后重试");
42:   const cockpitRoot = root.closest(".creator-cockpit");
43:   const backgroundControls = cockpitRoot?.querySelectorAll(
44:     ".cockpit-hero-actions, .cockpit-stage-strip, .cockpit-nav-band, .cockpit-copilot",
45:   );
46:   cockpitRoot?.classList.add("mechanism-workbench-open");
47:   backgroundControls?.forEach((element) => {
48:     element.inert = true;
49:   });
50:   return openCreatorMechanismWorkbench({

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 12 页 =====

01:     root,
02:     value:
03:       worldStore.get().cloudWorkspacePreview?.world?.settings?.mechanismDesign,
04:     onClose: () => {
05:       cockpitRoot?.classList.remove("mechanism-workbench-open");
06:       backgroundControls?.forEach((element) => {
07:         element.inert = false;
08:       });
09:       callView("creatorCockpit", "rerenderCockpit");
10:     },
11:     onSave: async (mechanismDesign) => {
12:       try {
13:         await zhimuApi.patchWorld({ settings: { mechanismDesign } }, worldId);
14:         callRuntime("invalidateStudioSnapshot", { clear: true });
15:         callView("creatorCockpit", "invalidateCockpitData");
16:         await callView("creatorCockpit", "refreshCockpitData", { force: true });
17:         showToast(
18:           mechanismDesign.status === "confirmed"
19:             ? "机制设计已确认并进入生成上下文"
20:             : "机制设计草稿已保存",
21:         );
22:       } catch (error) {
23:         showToast(error?.message || "机制设计保存失败");
24:         throw error;
25:       }
26:     },
27:   });
28: }
29:
30: // ===== 文件结束：src/views/creator-mechanism-workbench.js =====
31:
32: // ===== 文件：src/views/creator-mechanism-workbench.css =====
33: .creator-mechanism-workbench {
34:   width: 100%;
35:   overflow: hidden;
36:   border: 1px solid var(--line);
37:   border-radius: 18px;
38:   background: var(--surface);
39: }
40: .creator-cockpit.mechanism-workbench-open .cockpit-hero-actions,
41: .creator-cockpit.mechanism-workbench-open .cockpit-stage-strip,
42: .creator-cockpit.mechanism-workbench-open .cockpit-nav-band,
43: .creator-cockpit.mechanism-workbench-open .cockpit-copilot {
44:   pointer-events: none;
45:   opacity: 0.55;
46: }
47: .mechanism-workbench-head {
48:   position: sticky;
49:   top: 0;
50:   z-index: 2;

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 13 页 =====

01:   display: flex;
02:   justify-content: space-between;
03:   gap: 20px;
04:   padding: 26px 30px;
05:   border-bottom: 1px solid var(--line);
06:   background: color-mix(in srgb, var(--surface) 94%, #e1efe8);
07: }
08: .mechanism-workbench-head p {
09:   margin: 0 0 5px;
10:   color: var(--accent);
11:   font-size: 11px;
12:   font-weight: 900;
13:   letter-spacing: 0.18em;
14: }
15: .mechanism-workbench-head h2 {
16:   margin: 0 0 6px;
17: }
18: .mechanism-workbench-head span {
19:   color: var(--muted);
20: }
21: .mechanism-coverage {
22:   flex: 0 0 auto;
23:   min-width: 92px;
24:   padding: 12px 16px;
25:   border-radius: 15px;
26:   background: #0e3b34;
27:   color: #fff;
28:   text-align: center;
29: }
30: .mechanism-coverage strong,
31: .mechanism-coverage small {
32:   display: block;
33: }
34: .mechanism-coverage strong {
35:   font-size: 24px;
36: }
37: .mechanism-coverage small {
38:   margin-top: 3px;
39:   opacity: 0.75;
40: }
41: .mechanism-design-section {
42:   display: grid;
43:   gap: 18px;
44:   padding: 26px 30px;
45:   border-bottom: 1px solid var(--line);
46: }
47: .mechanism-design-section-head {
48:   display: flex;
49:   justify-content: space-between;
50:   align-items: end;

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 14 页 =====

01:   gap: 20px;
02: }
03: .mechanism-design-section-head > div {
04:   display: flex;
05:   align-items: center;
06:   gap: 10px;
07: }
08: .mechanism-design-section-head span {
09:   display: grid;
10:   width: 30px;
11:   height: 30px;
12:   place-items: center;
13:   border-radius: 10px;
14:   background: #0e3b34;
15:   color: #fff;
16:   font-size: 12px;
17:   font-weight: 800;
18: }
19: .mechanism-design-section-head h3,
20: .mechanism-design-section-head p {
21:   margin: 0;
22: }
23: .mechanism-design-section-head p {
24:   max-width: 520px;
25:   color: var(--muted);
26:   font-size: 13px;
27:   text-align: right;
28: }
29: .mechanism-design-card-grid {
30:   display: grid;
31:   grid-template-columns: repeat(3, minmax(0, 1fr));
32:   gap: 10px;
33: }
34: .mechanism-design-card {
35:   display: grid;
36:   grid-template-columns: auto 1fr;
37:   gap: 3px 9px;
38:   padding: 15px;
39:   border: 1px solid var(--line);
40:   border-radius: 15px;
41:   background: var(--surface);
42:   color: var(--ink);
43:   text-align: left;
44:   cursor: pointer;
45: }
46: .mechanism-design-card > span {
47:   grid-row: 1/3;
48:   align-self: start;
49:   padding: 5px 7px;
50:   border-radius: 8px;

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 15 页 =====

01:   background: color-mix(in srgb, var(--accent) 10%, transparent);
02:   color: var(--accent);
03:   font-size: 11px;
04:   font-weight: 800;
05: }
06: .mechanism-design-card strong {
07:   font-size: 15px;
08: }
09: .mechanism-design-card p {
10:   grid-column: 2;
11:   margin: 0;
12:   color: var(--muted);
13:   font-size: 12px;
14:   line-height: 1.55;
15: }
16: .mechanism-design-card.active {
17:   border-color: var(--accent);
18:   box-shadow: 0 0 0 2px color-mix(in srgb, var(--accent) 13%, transparent);
19:   background: color-mix(in srgb, var(--surface) 94%, #dcefe8);
20: }
21: .mechanism-cross-surface-preview {
22:   display: grid;
23:   grid-template-columns: repeat(2, minmax(0, 1fr));
24:   gap: 10px;
25: }
26: .mechanism-cross-surface-preview article {
27:   padding: 13px 15px;
28:   border-radius: 12px;
29:   background: color-mix(in srgb, var(--surface) 90%, #e6eee9);
30: }
31: .mechanism-cross-surface-preview span {
32:   color: var(--accent);
33:   font-size: 11px;
34:   font-weight: 900;
35: }
36: .mechanism-cross-surface-preview p {
37:   margin: 5px 0 0;
38:   font-size: 13px;
39:   line-height: 1.6;
40: }
41: .mechanism-design-basics,
42: .mechanism-design-question-grid {
43:   display: grid;
44:   grid-template-columns: repeat(2, minmax(0, 1fr));
45:   gap: 12px;
46: }
47: .mechanism-design-basics label,
48: .mechanism-design-field {
49:   display: grid;
50:   gap: 7px;

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 16 页 =====

01: }
02: .mechanism-design-basics span,
03: .mechanism-design-field > span {
04:   font-size: 13px;
05:   font-weight: 800;
06: }
07: .mechanism-design-basics input,
08: .mechanism-design-basics textarea,
09: .mechanism-design-field textarea {
10:   width: 100%;
11:   box-sizing: border-box;
12:   padding: 11px 12px;
13:   border: 1px solid var(--line);
14:   border-radius: 11px;
15:   background: var(--surface);
16:   color: var(--ink);
17:   font: inherit;
18:   resize: vertical;
19: }
20: .mechanism-design-field small {
21:   min-height: 34px;
22:   color: var(--muted);
23:   line-height: 1.5;
24: }
25: .mechanism-design-field.wide {
26:   grid-column: 1/-1;
27: }
28: .mechanism-design-basics .is-invalid input,
29: .mechanism-design-basics .is-invalid textarea,
30: .mechanism-design-field.is-invalid textarea {
31:   border-color: #b42318;
32:   box-shadow: 0 0 0 2px color-mix(in srgb, #b42318 12%, transparent);
33: }
34: .mechanism-validation-message {
35:   margin: 18px 30px 0;
36:   padding: 14px 16px;
37:   border: 1px solid color-mix(in srgb, #b42318 40%, var(--line));
38:   border-radius: 12px;
39:   background: color-mix(in srgb, #b42318 7%, var(--surface));
40:   color: var(--ink);
41: }
42: .mechanism-validation-message strong,
43: .mechanism-validation-message p {
44:   display: block;
45:   margin: 0;
46: }
47: .mechanism-validation-message p,
48: .mechanism-validation-message ul {
49:   margin-top: 6px;
50: }

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 17 页 =====

01: .mechanism-validation-message ul {
02:   margin-bottom: 0;
03:   padding-left: 20px;
04:   color: #8a1c13;
05:   font-size: 13px;
06:   line-height: 1.6;
07: }
08: .mechanism-validation-message.is-save-error {
09:   border-color: color-mix(in srgb, #a15c00 45%, var(--line));
10:   background: color-mix(in srgb, #a15c00 8%, var(--surface));
11: }
12: .mechanism-workbench-actions {
13:   position: sticky;
14:   bottom: 0;
15:   z-index: 2;
16:   display: flex;
17:   justify-content: space-between;
18:   align-items: center;
19:   gap: 20px;
20:   padding: 18px 30px;
21:   background: color-mix(in srgb, var(--surface) 95%, #edf3ef);
22:   border-top: 1px solid var(--line);
23: }
24: .mechanism-workbench-actions > div {
25:   display: flex;
26:   align-items: center;
27:   justify-content: flex-end;
28:   gap: 10px;
29: }
30: .mechanism-workbench-actions span {
31:   max-width: 360px;
32:   color: var(--muted);
33:   font-size: 12px;
34:   line-height: 1.5;
35:   text-align: right;
36: }
37: @media (max-width: 760px) {
38:   .mechanism-workbench-head,
39:   .mechanism-design-section,
40:   .mechanism-validation-message,
41:   .mechanism-workbench-actions {
42:     padding-left: 18px;
43:     padding-right: 18px;
44:   }
45:   .mechanism-validation-message {
46:     margin-left: 18px;
47:     margin-right: 18px;
48:   }
49:   .mechanism-design-card-grid,
50:   .mechanism-design-basics,

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 18 页 =====

01:   .mechanism-design-question-grid,
02:   .mechanism-cross-surface-preview {
03:     grid-template-columns: 1fr;
04:   }
05:   .mechanism-design-section-head,
06:   .mechanism-workbench-actions,
07:   .mechanism-workbench-actions > div {
08:     align-items: stretch;
09:     flex-direction: column;
10:   }
11:   .mechanism-design-section-head p,
12:   .mechanism-workbench-actions span {
13:     text-align: left;
14:   }
15:   .mechanism-workbench-actions > div {
16:     display: grid;
17:     grid-template-columns: 1fr 1fr;
18:   }
19:   .mechanism-workbench-actions span {
20:     grid-column: 1/-1;
21:   }
22:   .mechanism-design-field.wide {
23:     grid-column: auto;
24:   }
25: }
26:
27: // ===== 文件结束：src/views/creator-mechanism-workbench.css =====
28:
29: // ===== 文件：src/runtime/actions-creator-cockpit.js =====
30: /** Creator cockpit actions — native CRUD, navigation, refresh. */
31: import * as zhimuApi from "../api/index.js";
32: import { showToast } from "../components/toast.js";
33: import { normalizeError } from "../components/status-ui.js";
34: import { worldStore } from "../state/index.js";
35: import { LOGLINE_TEMPLATE, newSparkId } from "../views/creator-cockpit-model.js";
36: import { clueGrantsFromText } from "../views/creator-cockpit-segment.js";
37: import { normalizeSegmentOperations } from "shared/segment-contract.js";
38: import {
39:   confirmStorySpineSection,
40:   normalizeStorySpine
41: } from "shared/story-spine.js";
42: import { callView } from "./view-registry.js";
43: import { ownsCreatorCockpitAction } from "./action-ownership.js";
44: import { callRuntime } from "./runtime-facade.js";
45:
46: const showError = (error, fallback = "操作失败") => showToast(normalizeError(error, fallback));
47:
48: (function (window) {
49:   function cockpitRoot() {
50:     return document.querySelector(".creator-cockpit");

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 19 页 =====

01:   }
02:
03:   function field(name, attr = "data-cockpit-truth") {
04:     return cockpitRoot()?.querySelector(`[${attr}="${name}"]`)?.value?.trim?.() ?? cockpitRoot()?.que ...
05:   }
06:
07:   function workspacePreview() {
08:     return worldStore.get().cloudWorkspacePreview;
09:   }
10:
11:   async function reloadCockpitAfterWrite() {
12:     callRuntime("invalidateStudioSnapshot", { clear: true });
13:     callView("creatorCockpit", "invalidateCockpitData");
14:     await callView("creatorCockpit", "refreshCockpitData", { force: true });
15:   }
16:
17:   function maybeAutoLoadCockpit(view) {
18:     if (view !== "creatorCockpit") return;
19:     const worldId = zhimuApi.context.worldId;
20:     const worldValidated = (worldStore.get().cloudWorlds || []).some((world) => world.id === worldId);
21:     if (!worldId || !worldValidated) return;
22:     // Enter-once load; refreshCockpitData no-ops when already loaded for this world.
23:     void callView("creatorCockpit", "refreshCockpitData");
24:   }
25:
26:   async function handleCreatorCockpitAction(action, el) {
27:     if (!ownsCreatorCockpitAction(action)) return false;
28:     const worldId = zhimuApi.context.worldId;
29:     if (!worldId && action !== "cockpit-refresh") {
30:       showToast("请先选择剧本");
31:       return true;
32:     }
33:
34:     switch (action) {
35:       case "cockpit-refresh":
36:         callView("creatorCockpit", "invalidateCockpitData");
37:         void callView("creatorCockpit", "refreshCockpitData", { force: true });
38:         return true;
39:
40:       case "cockpit-goto-target":
41:         callView("creatorCockpit", "navigateCockpit", { target: el?.dataset?.cockpitTarget });
42:         return true;
43:
44:       case "cockpit-open-mechanism-workbench":
45:         await (await import("../views/creator-mechanism-workbench.js"))
46:           .openCurrentCreatorMechanismWorkbench();
47:         return true;
48:
49:       case "cockpit-fill-logline-template": {
50:         callView("creatorCockpit", "patchCockpitDraft", {

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 20 页 =====

01:           logline: LOGLINE_TEMPLATE,
02:           activeCanvas: "logline",
03:           activeItem: "logline"
04:         });
05:         callView("creatorCockpit", "scheduleSummarySave");
06:         callView("creatorCockpit", "rerenderCockpit");
07:         return true;
08:       }
09:
10:       case "cockpit-story-spine-assemble": {
11:         const draft = callView("creatorCockpit", "getCockpitDraft") || {};
12:         if (draft.storySpineAssembling) return true;
13:         callView("creatorCockpit", "patchCockpitDraft", { storySpineAssembling: true });
14:         callView("creatorCockpit", "rerenderCockpit");
15:         try {
16:           const result = await zhimuApi.assembleStorySpine({
17:             creatorInput: {
18:               logline: draft.logline || "",
19:               sparks: draft.sparks || [],
20:               sellingPoints: draft.sellingPoints || [],
21:               target: draft.target || "",
22:               duration: draft.duration || "",
23:               type: draft.type || "",
24:               focus: "把现有材料装配成一份能够概览整体故事、继续进入角色与章节生产的故事主干。"
25:             }
26:           }, worldId);
27:           callView("creatorCockpit", "patchCockpitDraft", {
28:             storySpineCandidate: normalizeStorySpine(result?.storySpine),
29:             storySpineAssembling: false,
30:             activeStage: "concept",
31:             activeItem: "story",
32:             activeCanvas: "story"
33:           });
34:           callView("creatorCockpit", "rerenderCockpit");
35:           showToast("故事主干候选已经组装完成，请检查后再采用");
36:         } catch (error) {
37:           callView("creatorCockpit", "patchCockpitDraft", { storySpineAssembling: false });
38:           callView("creatorCockpit", "rerenderCockpit");
39:           showError(error, "故事总览组装失败");
40:         }
41:         return true;
42:       }
43:
44:       case "cockpit-story-spine-adopt": {
45:         const draft = callView("creatorCockpit", "getCockpitDraft") || {};
46:         if (!draft.storySpineCandidate) return true;
47:         try {
48:           await zhimuApi.patchWorld({
49:             settings: { storySpine: normalizeStorySpine(draft.storySpineCandidate) }
50:           }, worldId);

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 21 页 =====

01:           callView("creatorCockpit", "patchCockpitDraft", { storySpineCandidate: null });
02:           await reloadCockpitAfterWrite();
03:           showToast("候选版本已采用为当前故事总览");
04:         } catch (error) {
05:           showError(error, "故事总览保存失败");
06:         }
07:         return true;
08:       }
09:
10:       case "cockpit-story-spine-discard":
11:         callView("creatorCockpit", "patchCockpitDraft", { storySpineCandidate: null });
12:         callView("creatorCockpit", "rerenderCockpit");
13:         showToast("已放弃候选版本，当前故事总览没有变化");
14:         return true;
15:
16:       case "cockpit-story-spine-confirm": {
17:         const sectionKey = el?.dataset?.storySpineKey;
18:         const current = workspacePreview()?.world?.settings?.storySpine;
19:         if (!sectionKey || !current) return true;
20:         try {
21:           const storySpine = confirmStorySpineSection(current, sectionKey);
22:           await zhimuApi.patchWorld({ settings: { storySpine } }, worldId);
23:           await reloadCockpitAfterWrite();
24:           showToast("该区块已确认为作者设定，后续装配会保留原文");
25:         } catch (error) {
26:           showError(error, "作者设定确认失败");
27:         }
28:         return true;
29:       }
30:
31:       case "cockpit-add-spark": {
32:         const draft = callView("creatorCockpit", "getCockpitDraft") || {};
33:         const text = String(draft.sparkDraft || "").trim();
34:         if (!text) return showToast("先写一句灵感"), true;
35:         const sparks = [...(draft.sparks || []), { id: newSparkId(), text, tag: draft.sparkTag || "灵感 ...
36:         callView("creatorCockpit", "patchCockpitDraft", { sparks, sparkDraft: "" });
37:         callView("creatorCockpit", "scheduleBriefSave");
38:         callView("creatorCockpit", "rerenderCockpit");
39:         showToast("灵感已记录");
40:         return true;
41:       }
42:
43:       case "cockpit-remove-spark": {
44:         const draft = callView("creatorCockpit", "getCockpitDraft") || {};
45:         const sparks = (draft.sparks || []).filter((s) => s.id !== el?.dataset?.sparkId);
46:         callView("creatorCockpit", "patchCockpitDraft", { sparks });
47:         callView("creatorCockpit", "scheduleBriefSave");
48:         callView("creatorCockpit", "rerenderCockpit");
49:         return true;
50:       }

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 22 页 =====

01:
02:       case "cockpit-adopt-spark": {
03:         const draft = callView("creatorCockpit", "getCockpitDraft") || {};
04:         const spark = (draft.sparks || []).find((s) => s.id === el?.dataset?.sparkId);
05:         if (!spark) return true;
06:         const logline = draft.logline?.trim() ? `${draft.logline.trim()}\n${spark.text}` : spark.text;
07:         callView("creatorCockpit", "patchCockpitDraft", { logline, activeStage: "concept", activeItem ...
08:         callView("creatorCockpit", "scheduleSummarySave");
09:         callView("creatorCockpit", "rerenderCockpit");
10:         showToast("已写入梗概");
11:         return true;
12:       }
13:
14:       case "cockpit-add-truth-claim": {
15:         const title = field("title");
16:         const claim = field("claim");
17:         const confidence = cockpitRoot()?.querySelector('[data-cockpit-truth="confidence"]')?.value | ...
18:         if (!title || !claim) return showToast("请填写标题与断言"), true;
19:         try {
20:           await zhimuApi.createTruthClaim({ title, claim, confidence }, worldId);
21:           await reloadCockpitAfterWrite();
22:           showToast("核心事实已添加");
23:         } catch (error) {
24:           showError(error);
25:         }
26:         return true;
27:       }
28:
29:       case "cockpit-add-relationship": {
30:         const from = field("from", "data-cockpit-rel");
31:         const to = field("to", "data-cockpit-rel");
32:         const label = field("label", "data-cockpit-rel");
33:         const strengthRaw = cockpitRoot()?.querySelector('[data-cockpit-rel="strength"]')?.value;
34:         if (!from || !to || from === to) return showToast("请选择两个不同角色"), true;
35:         try {
36:           await zhimuApi.createRoleRelationship({
37:             fromRoleSlotId: from,
38:             toRoleSlotId: to,
39:             label,
40:             strength: strengthRaw === "" || strengthRaw == null ? undefined : Number(strengthRaw)
41:           }, worldId);
42:           await reloadCockpitAfterWrite();
43:           showToast("关系已添加");
44:         } catch (error) {
45:           showError(error);
46:         }
47:         return true;
48:       }
49:
50:       case "cockpit-add-chapter": {

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 23 页 =====

01:         const title = field("title", "data-cockpit-chapter");
02:         const summary = cockpitRoot()?.querySelector('[data-cockpit-chapter="summary"]')?.value?.trim ...
03:         if (!title) return showToast("请填写章节标题"), true;
04:         const chapters = workspacePreview()?.chapters || [];
05:         try {
06:           await zhimuApi.createChapter(worldId, { title, summary, sequence: chapters.length + 1 });
07:           await reloadCockpitAfterWrite();
08:           showToast("章节已添加");
09:         } catch (error) {
10:           showError(error);
11:         }
12:         return true;
13:       }
14:
15:       case "cockpit-add-role": {
16:         const name = field("name", "data-cockpit-role");
17:         if (!name) return showToast("请填写角色名"), true;
18:         const roles = workspacePreview()?.roles || [];
19:         try {
20:           await zhimuApi.createRole(worldId, { name, sequence: roles.length + 1 });
21:           await reloadCockpitAfterWrite();
22:           showToast("角色已添加");
23:         } catch (error) {
24:           showError(error);
25:         }
26:         return true;
27:       }
28:
29:       case "cockpit-add-section": {
30:         const roleId = cockpitRoot()?.querySelector('[data-cockpit-section="role"]')?.value;
31:         const chapterId = cockpitRoot()?.querySelector('[data-cockpit-section="chapter"]')?.value;
32:         const title = field("title", "data-cockpit-section");
33:         if (!roleId || !chapterId || !title) return showToast("请选择角色、章节并填写分幕标题"), true;
34:         try {
35:           await zhimuApi.createSection(worldId, roleId, {
36:             title,
37:             chapterId,
38:             body: `# ${title}\n\n（在此撰写私人分幕正文）`,
39:             sequence: (workspacePreview()?.sections || []).filter((s) => s.role_slot_id === roleId).l ...
40:           });
41:           await reloadCockpitAfterWrite();
42:           showToast("分幕已添加，可在创作台编辑正文");
43:         } catch (error) {
44:           showError(error);
45:         }
46:         return true;
47:       }
48:
49:       case "cockpit-sync-segments":
50:         try {

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 24 页 =====

01:           await zhimuApi.syncWorldSegmentsFromGraph(worldId);
02:           await reloadCockpitAfterWrite();
03:           showToast("已从章节同步运行段落");
04:         } catch (error) {
05:           showError(error);
06:         }
07:         return true;
08:
09:       case "cockpit-save-segment": {
10:         const segmentId = el?.dataset?.segmentId;
11:         const segment = (worldStore.get().cloudSegments || []).find((s) => s.id === segmentId);
12:         if (!segmentId || !segment) return showToast("请选择运行段落"), true;
13:         const root = cockpitRoot()?.querySelector(`[data-cockpit-segment-editor="${segmentId}"]`);
14:         if (!root) return true;
15:         const flow = root.querySelector('[data-cockpit-seg="flow"]')?.value || "";
16:         const hostTruth = root.querySelector('[data-cockpit-seg="hostTruth"]')?.value || "";
17:         const clueGrants = clueGrantsFromText(root.querySelector('[data-cockpit-seg="clueGrants"]')?. ...
18:         const operations = normalizeSegmentOperations({
19:           ...(segment.operations || {}),
20:           flow,
21:           hostTruth,
22:           clueGrants
23:         });
24:         try {
25:           await zhimuApi.updateWorldSegment(segmentId, {
26:             title: segment.title,
27:             sequence: segment.sequence,
28:             story: segment.story || {},
29:             operations
30:           }, worldId);
31:           await reloadCockpitAfterWrite();
32:           showToast("运行段落的主持手册已保存");
33:         } catch (error) {
34:           showError(error);
35:         }
36:         return true;
37:       }
38:
39:       case "cockpit-add-clue": {
40:         const name = field("name", "data-cockpit-clue");
41:         const publicText = cockpitRoot()?.querySelector('[data-cockpit-clue="publicText"]')?.value?.t ...
42:         if (!name) return showToast("请填写线索名称"), true;
43:         try {
44:           await zhimuApi.createClue({ name, publicText: publicText || undefined });
45:           await reloadCockpitAfterWrite();
46:           showToast("线索已添加");
47:         } catch (error) {
48:           showError(error);
49:         }
50:         return true;

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 25 页 =====

01:       }
02:
03:       case "cockpit-save-chapter-summary": {
04:         const chapterId = el?.dataset?.chapterId;
05:         const summary = cockpitRoot()?.querySelector(`[data-cockpit-chapter-summary="${chapterId}"]`) ...
06:         if (!chapterId) return true;
07:         const chapter = workspacePreview()?.chapters?.find((c) => c.id === chapterId);
08:         if (!chapter) return true;
09:         try {
10:           await zhimuApi.updateChapter(chapterId, {
11:             title: chapter.title,
12:             summary
13:           });
14:           await reloadCockpitAfterWrite();
15:           showToast("章节摘要已保存");
16:         } catch (error) {
17:           showError(error);
18:         }
19:         return true;
20:       }
21:
22:       case "cockpit-analyze-draft": {
23:         const draft = callView("creatorCockpit", "getCockpitDraft") || {};
24:         const text = String(draft.copilotQuery || draft.logline || "").trim();
25:         if (!text) return showToast("请先输入或粘贴文本"), true;
26:         try {
27:           const result = await zhimuApi.analyzeStoryDraft(text);
28:           callView("creatorCockpit", "patchCockpitDraft", { lastAnalysis: result, lastAiNote: "" });
29:           callView("creatorCockpit", "rerenderCockpit");
30:           showToast(`结构识别完成 · ${result.nodes?.length || 0} 个节点`);
31:         } catch (error) {
32:           showError(error);
33:         }
34:         return true;
35:       }
36:
37:       case "cockpit-ai-suggest": {
38:         const draft = callView("creatorCockpit", "getCockpitDraft") || {};
39:         const studio = workspacePreview();
40:         const text = String(draft.copilotQuery || draft.logline || "").trim();
41:         if (!text) return showToast("请先输入或粘贴文本"), true;
42:         try {
43:           const status = await zhimuApi.getDeepseekStatus?.().catch(() => ({ configured: false }));
44:           if (!status?.configured) {
45:             showToast("尚未配置 LLM，请先在账号中连接 API Key");
46:             return true;
47:           }
48:           showToast("AI 正在生成…");
49:           const result = await zhimuApi.proposeWithDeepseek({
50:             title: studio?.world?.name || "当前剧本",

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 26 页 =====

01:             premise: draft.logline || text,
02:             requirements: `${text}\n\n当前阶段关注：${draft.activeStage || "concept"}`,
03:             skipOutline: true
04:           });
05:           const note = result?.proposal?.chapters?.[0]?.summary
06:             || result?.proposal?.scenes?.[0]?.publicText
07:             || result?.proposal?.logline
08:             || JSON.stringify(result?.proposal || {}).slice(0, 600);
09:           callView("creatorCockpit", "patchCockpitDraft", { lastAiNote: String(note) });
10:           callView("creatorCockpit", "rerenderCockpit");
11:           showToast("AI 输出已生成");
12:         } catch (error) {
13:           showError(error, "AI 续写失败");
14:         }
15:         return true;
16:       }
17:
18:       default:
19:         return false;
20:     }
21:   }
22:
23:   async function handleSectionStatusChange(selectEl) {
24:     const roleId = selectEl?.dataset?.roleId;
25:     const sectionId = selectEl?.dataset?.sectionId;
26:     const publicationStatus = selectEl?.value;
27:     const studio = await callRuntime("ensureStudioSnapshot");
28:     const section = studio?.sections?.find((s) => s.id === sectionId);
29:     if (!roleId || !sectionId || !section) return;
30:     try {
31:       await zhimuApi.updateSection(roleId, sectionId, {
32:         title: section.title,
33:         body: section.body,
34:         chapterId: section.chapter_id || section.chapterId || null,
35:         sequence: section.sequence,
36:         publicationStatus
37:       });
38:       await reloadCockpitAfterWrite();
39:       showToast("分幕状态已更新");
40:     } catch (error) {
41:       showError(error);
42:     }
43:   }
44:
45:   document.addEventListener("change", (event) => {
46:     const selectEl = event.target.closest(".creator-cockpit select[data-section-id]");
47:     if (!selectEl) return;
48:     void handleSectionStatusChange(selectEl);
49:   });
50:

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 27 页 =====

01:   window.zhimuActionsCreatorCockpit = { handleCreatorCockpitAction, maybeAutoLoadCockpit };
02: })(window);
03:
04: export function maybeAutoLoadCockpit(view) {
05:   window.zhimuActionsCreatorCockpit?.maybeAutoLoadCockpit?.(view);
06: }
07:
08: export function handleCreatorCockpitAction(action, el) {
09:   return window.zhimuActionsCreatorCockpit?.handleCreatorCockpitAction?.(action, el) || false;
10: }
11:
12: // ===== 文件结束：src/runtime/actions-creator-cockpit.js =====
13:
14: // ===== 文件：backend/src/room-mechanism-runtime-service.js =====
15: import { createHash } from "node:crypto";
16: import { throwErr } from "./api-errors.js";
17: import { logHostAction } from "./audit-log.js";
18: import { canonicalReleaseJson } from "./world-release-contract.js";
19: import {
20:   MechanismRuntimeError,
21:   analyzeMechanismRuntimeReachability,
22:   advanceMechanismRound,
23:   executeMechanismDecision,
24:   executeMechanismInvestigation,
25:   executeMechanismOverride,
26:   initializeMechanismRuntime,
27:   projectMechanismRuntime,
28: } from "./mechanism-runtime.js";
29: import { loadRuntimeContentProvider } from "./runtime-content-provider.js";
30: import { transactionWithEvents } from "./transaction-events.js";
31: import {
32:   appendRoomMechanismAction,
33:   findRoomMechanismState,
34:   insertRoomMechanismState,
35:   listRoomMechanismActions,
36:   replaceRoomMechanismState,
37:   updateRoomMechanismRuntime,
38: } from "./repositories/room-mechanism-runtime-repository.js";
39: import {
40:   configureHostContentActionTransaction,
41:   hasActiveHostMembership,
42: } from "./repositories/host-content-action-repository.js";
43: import { getRoomMechanismSubmissionSummary } from "./room-mechanism-submission-service.js";
44: import { normalizeMechanismInteraction } from "../../shared/mechanism-interactions.js";
45:
46: function packageChecksum(packageValue) {
47:   return createHash("sha256")
48:     .update(canonicalReleaseJson(packageValue), "utf8")
49:     .digest("hex");
50: }

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 28 页 =====

01:
02: function bindingFromProvider(provider, packageValue) {
03:   return {
04:     contentBindingMode: provider.contentBinding.mode,
05:     contentReleaseId: provider.contentBinding.release?.id ?? null,
06:     sourceContentRevision: Number(provider.sourceRevision),
07:     mechanismPackageSha256: packageChecksum(packageValue),
08:   };
09: }
10:
11: function bindingMatches(state, binding) {
12:   return (
13:     state.contentBindingMode === binding.contentBindingMode &&
14:     String(state.contentReleaseId ?? "") ===
15:       String(binding.contentReleaseId ?? "") &&
16:     state.sourceContentRevision === binding.sourceContentRevision &&
17:     state.mechanismPackageSha256 === binding.mechanismPackageSha256
18:   );
19: }
20:
21: async function assertHostAccess(client, roomId, actorId) {
22:   if (!(await hasActiveHostMembership(client, { roomId, actorId })))
23:     throwErr("HOST_ROLE_REQUIRED");
24: }
25:
26: async function loadContext(roomId, client = null) {
27:   const runQuery = client?.query ? client.query.bind(client) : undefined;
28:   const provider = await loadRuntimeContentProvider(roomId, {
29:     ...(runQuery ? { runQuery } : {}),
30:     includeLiveSnapshot: true,
31:   });
32:   if (!provider) throwErr("ROOM_NOT_FOUND");
33:   const packageValue = provider.snapshot?.mechanismPackage ?? null;
34:   if (!packageValue) throwErr("MECHANISM_PACKAGE_NOT_FOUND");
35:   return {
36:     provider,
37:     packageValue,
38:     binding: bindingFromProvider(provider, packageValue),
39:   };
40: }
41:
42: function translateRuntimeError(error) {
43:   if (!(error instanceof MechanismRuntimeError)) throw error;
44:   const details = { runtimeCode: error.code, ...(error.details ?? {}) };
45:   if (
46:     [
47:       "MECHANISM_STATE_INVALID",
48:       "MECHANISM_RESOURCE_INVALID",
49:       "MECHANISM_RESOURCE_OUT_OF_BOUNDS",
50:       "MECHANISM_OPERATION_UNSUPPORTED",

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 29 页 =====

01:       "MECHANISM_EFFECT_INVALID",
02:     ].includes(error.code)
03:   )
04:     throwErr("MECHANISM_TRANSITION_INVALID", error.message, details);
05:   if (
06:     [
07:       "MECHANISM_OPTION_INVALID",
08:       "MECHANISM_STATE_UNKNOWN",
09:       "MECHANISM_RESOURCE_UNKNOWN",
10:       "MECHANISM_ROUND_UNKNOWN",
11:     ].includes(error.code)
12:   )
13:     throwErr("MECHANISM_ACTION_INVALID", error.message, details);
14:   throwErr("MECHANISM_ACTION_BLOCKED", error.message, details);
15: }
16:
17: function runtimeResponse(state, packageValue, provider, extra = {}) {
18:   const stale = state
19:     ? !bindingMatches(state, bindingFromProvider(provider, packageValue))
20:     : false;
21:   return {
22:     initialized: Boolean(state),
23:     roomId: provider.room.id,
24:     worldId: provider.worldId,
25:     contentBinding: provider.contentBinding,
26:     stale,
27:     submissionSummary: extra.submissionSummary ?? [],
28:     state: state
29:       ? {
30:           revision: state.revision,
31:           initializedAt: state.initializedAt,
32:           roundStartedAt: state.roundStartedAt,
33:           updatedAt: state.updatedAt,
34:           ...projectMechanismRuntime(state.runtime, packageValue),
35:           reachability: stale
36:             ? null
37:             : analyzeMechanismRuntimeReachability(state.runtime, packageValue),
38:         }
39:       : null,
40:     ...extra,
41:   };
42: }
43:
44: export function inspectMechanismDeadlineDefault({
45:   state,
46:   packageValue,
47:   action,
48:   now = new Date(),
49: }) {
50:   if (action?.type !== "decision") {

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 前30页 第 30 页 =====

01:     return { required: false, allowed: true, reason: "not_deadline_action" };
02:   }
03:   const decision = projectMechanismRuntime(
04:     state?.runtime,
05:     packageValue,
06:   ).availableDecisions.find((entry) => entry.key === action?.decisionKey);
07:   if (!decision) {
08:     return { required: true, allowed: false, reason: "decision_unavailable" };
09:   }
10:   const interaction = normalizeMechanismInteraction(decision.interaction);
11:   if (interaction.kind !== "timed_crisis") {
12:     return action?.source === "deadline_default"
13:       ? { required: true, allowed: false, reason: "not_timed" }
14:       : { required: false, allowed: true, reason: "not_timed" };
15:   }
16:   if (interaction.deadlineSeconds <= 0 || !interaction.defaultOptionKey) {
17:     return { required: true, allowed: false, reason: "default_missing" };
18:   }
19:   if (
20:     action?.source === "deadline_default" &&
21:     interaction.defaultOptionKey !== action?.optionKey
22:   ) {
23:     return { required: true, allowed: false, reason: "default_mismatch" };
24:   }
25:   if (!state?.roundStartedAt) {
26:     return { required: true, allowed: false, reason: "clock_missing" };
27:   }
28:   const startedAt = new Date(state.roundStartedAt).getTime();
29:   if (!Number.isFinite(startedAt)) {
30:     return { required: true, allowed: false, reason: "clock_missing" };
31:   }
32:   const deadlineAt = new Date(
33:     startedAt + interaction.deadlineSeconds * 1000,
34:   ).toISOString();
35:   const expired = new Date(now).getTime() >= new Date(deadlineAt).getTime();
36:   if (action?.source === "deadline_default" && !expired) {
37:     return {
38:       required: true,
39:       allowed: false,
40:       reason: "too_early",
41:       deadlineAt,
42:     };
43:   }
44:   if (action?.source !== "deadline_default" && expired) {
45:     return {
46:       required: true,
47:       allowed: false,
48:       reason: "deadline_expired",
49:       deadlineAt,
50:       defaultOptionKey: interaction.defaultOptionKey,

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 31 页 =====

01:   current_round_sequence integer CHECK (current_round_sequence IS NULL OR current_round_sequence > 0),
02:   prepared_round_key text,
03:   current_branch text,
04:   current_variant_key text,
05:   state_values jsonb NOT NULL DEFAULT '{}'::jsonb,
06:   resource_values jsonb NOT NULL DEFAULT '{}'::jsonb,
07:   evidence_states jsonb NOT NULL DEFAULT '{}'::jsonb,
08:   event_states jsonb NOT NULL DEFAULT '{}'::jsonb,
09:   decision_states jsonb NOT NULL DEFAULT '{}'::jsonb,
10:   executed_investigations jsonb NOT NULL DEFAULT '{}'::jsonb,
11:   ending jsonb,
12:   revision bigint NOT NULL DEFAULT 1 CHECK (revision > 0),
13:   initialized_by_user_id uuid REFERENCES users(id) ON DELETE SET NULL,
14:   initialized_at timestamptz NOT NULL DEFAULT now(),
15:   updated_at timestamptz NOT NULL DEFAULT now(),
16:   metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
17:   CHECK (
18:     (content_binding_mode = 'release' AND content_release_id IS NOT NULL)
19:     OR (content_binding_mode = 'live_draft' AND content_release_id IS NULL)
20:   ),
21:   CHECK (jsonb_typeof(state_values) = 'object'),
22:   CHECK (jsonb_typeof(resource_values) = 'object'),
23:   CHECK (jsonb_typeof(evidence_states) = 'object'),
24:   CHECK (jsonb_typeof(event_states) = 'object'),
25:   CHECK (jsonb_typeof(decision_states) = 'object'),
26:   CHECK (jsonb_typeof(executed_investigations) = 'object'),
27:   CHECK (ending IS NULL OR jsonb_typeof(ending) = 'object'),
28:   CHECK (jsonb_typeof(metadata) = 'object')
29: );
30:
31: CREATE INDEX room_mechanism_states_binding_idx
32:   ON room_mechanism_states(content_release_id, source_content_revision);
33:
34: CREATE TABLE room_mechanism_action_log (
35:   id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
36:   room_id uuid NOT NULL REFERENCES rooms(id) ON DELETE CASCADE,
37:   actor_user_id uuid REFERENCES users(id) ON DELETE SET NULL,
38:   revision_before bigint NOT NULL CHECK (revision_before >= 0),
39:   revision_after bigint NOT NULL CHECK (revision_after > revision_before),
40:   round_key text,
41:   action_type text NOT NULL CHECK (action_type IN ('initialize', 'reset', 'decision', 'investigation' ...
42:   action_key text,
43:   option_key text,
44:   changes jsonb NOT NULL DEFAULT '[]'::jsonb,
45:   request jsonb NOT NULL DEFAULT '{}'::jsonb,
46:   metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
47:   created_at timestamptz NOT NULL DEFAULT now(),
48:   UNIQUE (room_id, revision_after),
49:   CHECK (jsonb_typeof(changes) = 'array'),
50:   CHECK (jsonb_typeof(request) = 'object'),

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 32 页 =====

01:   CHECK (jsonb_typeof(metadata) = 'object')
02: );
03:
04: CREATE INDEX room_mechanism_action_log_room_created_idx
05:   ON room_mechanism_action_log(room_id, created_at DESC);
06:
07: ALTER TABLE room_mechanism_states ENABLE ROW LEVEL SECURITY;
08: ALTER TABLE room_mechanism_action_log ENABLE ROW LEVEL SECURITY;
09:
10: COMMENT ON TABLE room_mechanism_states IS
11:   'Mutable per-room mechanism execution state bound to one live revision or immutable release.';
12: COMMENT ON TABLE room_mechanism_action_log IS
13:   'Append-only revision ledger for host decisions, investigations, advances and explicit overrides.';
14:
15: // ===== 文件结束：backend/migrations/106_room_mechanism_runtime.sql =====
16:
17: // ===== 文件：backend/migrations/107_room_mechanism_decision_submissions.sql =====
18: -- Per-role submissions for an unresolved authored mechanism decision. The
19: -- interaction contract decides whether they are advisory or private; the host
20: -- remains the authoritative runtime executor in both cases.
21: CREATE TABLE room_mechanism_decision_submissions (
22:   room_id uuid NOT NULL REFERENCES rooms(id) ON DELETE CASCADE,
23:   runtime_initialized_at timestamptz NOT NULL,
24:   mechanism_revision integer NOT NULL CHECK (mechanism_revision > 0),
25:   round_key text NOT NULL CHECK (length(round_key) BETWEEN 1 AND 160),
26:   decision_key text NOT NULL CHECK (length(decision_key) BETWEEN 1 AND 160),
27:   role_slot_id uuid NOT NULL REFERENCES role_slots(id) ON DELETE CASCADE,
28:   actor_user_id uuid REFERENCES users(id) ON DELETE SET NULL,
29:   option_key text NOT NULL CHECK (length(option_key) BETWEEN 1 AND 160),
30:   submitted_at timestamptz NOT NULL DEFAULT now(),
31:   updated_at timestamptz NOT NULL DEFAULT now(),
32:   PRIMARY KEY (room_id, runtime_initialized_at, decision_key, role_slot_id)
33: );
34:
35: CREATE INDEX room_mechanism_submissions_current_idx
36:   ON room_mechanism_decision_submissions(room_id, runtime_initialized_at, decision_key, updated_at DESC);
37:
38: CREATE INDEX room_mechanism_submissions_role_slot_idx
39:   ON room_mechanism_decision_submissions(role_slot_id);
40:
41: CREATE INDEX room_mechanism_submissions_actor_idx
42:   ON room_mechanism_decision_submissions(actor_user_id)
43:   WHERE actor_user_id IS NOT NULL;
44:
45: ALTER TABLE room_mechanism_decision_submissions ENABLE ROW LEVEL SECURITY;
46:
47: COMMENT ON TABLE room_mechanism_decision_submissions IS
48:   'Per-role advisory or private choices for the current room mechanism instance; hosts still perform  ...
49:
50: // ===== 文件结束：backend/migrations/107_room_mechanism_decision_submissions.sql =====

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 33 页 =====

01:
02: // ===== 文件：backend/migrations/108_room_mechanism_round_clock.sql =====
03: -- Server-owned start time for the current mechanism round. Timed decisions
04: -- derive their deadline from this value instead of trusting a player clock.
05: ALTER TABLE room_mechanism_states
06:   ADD COLUMN round_started_at timestamptz NOT NULL DEFAULT now();
07:
08: COMMENT ON COLUMN room_mechanism_states.round_started_at IS
09:   'Authoritative start time of the current mechanism round; reset only when the host advances or rein ...
10:
11: // ===== 文件结束：backend/migrations/108_room_mechanism_round_clock.sql =====
12:
13: // ===== 文件：backend/src/room-mechanism-submission-service.js =====
14: import { createHash } from "node:crypto";
15: import { throwErr } from "./api-errors.js";
16: import { canonicalReleaseJson } from "./world-release-contract.js";
17: import {
18:   projectMechanismRuntime,
19:   resolvePlayerMechanismSelection,
20: } from "./mechanism-runtime.js";
21: import { loadRuntimeContentProvider } from "./runtime-content-provider.js";
22: import { transactionWithEvents } from "./transaction-events.js";
23: import { findRoomMechanismState } from "./repositories/room-mechanism-runtime-repository.js";
24: import {
25:   listRoomMechanismSubmissions,
26:   upsertRoomMechanismSubmission,
27: } from "./repositories/room-mechanism-submission-repository.js";
28: import { normalizeMechanismInteraction } from "../../shared/mechanism-interactions.js";
29:
30: function packageChecksum(packageValue) {
31:   return createHash("sha256")
32:     .update(canonicalReleaseJson(packageValue), "utf8")
33:     .digest("hex");
34: }
35:
36: function sameBinding(state, provider, packageValue) {
37:   return (
38:     state.contentBindingMode === provider.contentBinding.mode &&
39:     String(state.contentReleaseId ?? "") ===
40:       String(provider.contentBinding.release?.id ?? "") &&
41:     state.sourceContentRevision === Number(provider.sourceRevision) &&
42:     state.mechanismPackageSha256 === packageChecksum(packageValue)
43:   );
44: }
45:
46: export function summarizeRoomMechanismSubmissions(rows = []) {
47:   const byDecision = new Map();
48:   for (const row of rows) {
49:     if (!byDecision.has(row.decisionKey)) {
50:       byDecision.set(row.decisionKey, {

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 34 页 =====

01:         decisionKey: row.decisionKey,
02:         total: 0,
03:         options: new Map(),
04:         roles: [],
05:       });
06:     }
07:     const summary = byDecision.get(row.decisionKey);
08:     summary.total += 1;
09:     summary.options.set(
10:       row.optionKey,
11:       (summary.options.get(row.optionKey) || 0) + 1,
12:     );
13:     summary.roles.push({
14:       roleSlotId: row.roleSlotId,
15:       roleName: row.roleName,
16:       optionKey: row.optionKey,
17:       updatedAt: row.updatedAt,
18:     });
19:   }
20:   return [...byDecision.values()].map((summary) => ({
21:     decisionKey: summary.decisionKey,
22:     total: summary.total,
23:     options: [...summary.options].map(([optionKey, count]) => ({
24:       optionKey,
25:       count,
26:     })),
27:     roles: summary.roles,
28:   }));
29: }
30:
31: export async function getRoomMechanismSubmissionSummary({
32:   roomId,
33:   state,
34:   client = null,
35: }) {
36:   if (!state?.initializedAt) return [];
37:   const rows = await listRoomMechanismSubmissions(roomId, state.initializedAt, {
38:     client,
39:   });
40:   return summarizeRoomMechanismSubmissions(rows);
41: }
42:
43: export function inspectMechanismSubmissionWindow({
44:   interaction,
45:   roundStartedAt,
46:   now = new Date(),
47: }) {
48:   const normalized = normalizeMechanismInteraction(interaction);
49:   if (normalized.deadlineSeconds <= 0) {
50:     return { open: true, deadlineAt: null };

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 35 页 =====

01:   }
02:   if (!roundStartedAt) {
03:     return { open: false, deadlineAt: null, reason: "clock_missing" };
04:   }
05:   const startedAt = new Date(roundStartedAt).getTime();
06:   if (!Number.isFinite(startedAt)) {
07:     return { open: false, deadlineAt: null, reason: "clock_invalid" };
08:   }
09:   const deadlineAt = new Date(startedAt + normalized.deadlineSeconds * 1000);
10:   return {
11:     open: new Date(now).getTime() < deadlineAt.getTime(),
12:     deadlineAt: deadlineAt.toISOString(),
13:     reason: new Date(now).getTime() < deadlineAt.getTime() ? "open" : "expired",
14:   };
15: }
16:
17: export async function submitRoomMechanismDecisionPreference({
18:   roomId,
19:   actorId,
20:   roleSlotId,
21:   expectedRevision,
22:   decisionKey,
23:   optionKey,
24:   now = () => new Date(),
25: }) {
26:   return transactionWithEvents(async (client, queueEvent) => {
27:     const provider = await loadRuntimeContentProvider(roomId, {
28:       runQuery: client.query.bind(client),
29:       includeLiveSnapshot: true,
30:     });
31:     if (!provider) throwErr("ROOM_NOT_FOUND");
32:     const packageValue = provider.snapshot?.mechanismPackage ?? null;
33:     if (!packageValue) throwErr("MECHANISM_PACKAGE_NOT_FOUND");
34:     const state = await findRoomMechanismState(roomId, {
35:       client,
36:       forUpdate: true,
37:     });
38:     if (!state) throwErr("MECHANISM_RUNTIME_NOT_INITIALIZED");
39:     if (!sameBinding(state, provider, packageValue))
40:       throwErr("MECHANISM_RUNTIME_CONTENT_MISMATCH");
41:     if (
42:       !Number.isInteger(expectedRevision) ||
43:       expectedRevision !== state.revision
44:     ) {
45:       throwErr("MECHANISM_RUNTIME_REVISION_CONFLICT", undefined, {
46:         expectedRevision,
47:         currentRevision: state.revision,
48:       });
49:     }
50:     const available = projectMechanismRuntime(

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 36 页 =====

01:       state.runtime,
02:       packageValue,
03:     ).availableDecisions;
04:     const selection = resolvePlayerMechanismSelection(
05:       available,
06:       decisionKey,
07:       optionKey,
08:     );
09:     if (!selection) throwErr("MECHANISM_DECISION_SUBMISSION_CLOSED");
10:     const { decision } = selection;
11:     const interaction = normalizeMechanismInteraction(decision.interaction);
12:     if (
13:       !["advisory_choice", "private_choice"].includes(
14:         interaction.submissionMode,
15:       )
16:     ) {
17:       throwErr("MECHANISM_DECISION_SUBMISSION_CLOSED");
18:     }
19:     const submissionWindow = inspectMechanismSubmissionWindow({
20:       interaction,
21:       roundStartedAt: state.roundStartedAt,
22:       now: now(),
23:     });
24:     if (!submissionWindow.open) {
25:       throwErr("MECHANISM_DECISION_SUBMISSION_CLOSED");
26:     }
27:     const submission = await upsertRoomMechanismSubmission(client, {
28:       roomId,
29:       runtimeInitializedAt: state.initializedAt,
30:       mechanismRevision: state.revision,
31:       roundKey: state.runtime.currentRoundKey,
32:       decisionKey: selection.decisionKey,
33:       roleSlotId,
34:       actorId,
35:       optionKey: selection.optionKey,
36:     });
37:     const summary = await getRoomMechanismSubmissionSummary({
38:       roomId,
39:       state,
40:       client,
41:     });
42:     queueEvent(roomId, "room.mechanism_submission_updated", {
43:       decisionKey: selection.decisionKey,
44:       submissionCount:
45:         summary.find(
46:           (entry) => entry.decisionKey === selection.decisionKey,
47:         )?.total ?? 0,
48:     });
49:     return {
50:       decisionKey,

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 37 页 =====

01:       optionKey,
02:       revision: state.revision,
03:       submittedAt: submission.updatedAt,
04:     };
05:   });
06: }
07:
08: // ===== 文件结束：backend/src/room-mechanism-submission-service.js =====
09:
10: // ===== 文件：backend/src/routes/player-progress-routes.js =====
11: import { withRoomIdempotency } from "../idempotency-helpers.js";
12: import {
13:   addPlayerNotebookEntry,
14:   completePlayerSection,
15:   getPlayerTimeline,
16:   removePlayerNotebookEntry,
17:   startPlayerSection,
18:   submitPlayerMiniGame,
19: } from "../player-progress-service.js";
20: import { submitRoomMechanismDecisionPreference } from "../room-mechanism-submission-service.js";
21: import { requireActor } from "../request-actor.js";
22: import { throwErr } from "../api-errors.js";
23: import { requireRoomRole } from "./route-guards.js";
24: import {
25:   completeSectionSchema,
26:   deleteNotebookEntrySchema,
27:   notebookEntrySchema,
28:   playerProgressRoomIdParams,
29:   submitMechanismDecisionSchema,
30:   submitMiniGameSchema,
31: } from "./schemas/player-progress.js";
32:
33: function requirePlayerRole(membership) {
34:   if (!membership.role_slot_id) throwErr("PLAYER_ROLE_REQUIRED");
35:   return membership.role_slot_id;
36: }
37:
38: export async function registerPlayerProgressRoutes(app) {
39:   app.post(
40:     "/api/rooms/:roomId/player/mechanism-decisions/:decisionKey/submissions",
41:     { schema: submitMechanismDecisionSchema },
42:     async (request) => {
43:       const actorId = requireActor(request);
44:       const { roomId, decisionKey } = request.params;
45:       const roleSlotId = requirePlayerRole(
46:         await requireRoomRole(actorId, roomId),
47:       );
48:       return withRoomIdempotency(
49:         roomId,
50:         request,

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 38 页 =====

01:         "player.mechanism_decision_submit",
02:         () =>
03:           submitRoomMechanismDecisionPreference({
04:             roomId,
05:             decisionKey,
06:             roleSlotId,
07:             actorId,
08:             ...request.body,
09:           }),
10:       );
11:     },
12:   );
13:
14:   app.post(
15:     "/api/rooms/game/submit",
16:     { schema: submitMiniGameSchema },
17:     async (request) => {
18:       const actorId = requireActor(request);
19:       const { roomId, answer } = request.body;
20:       const gameId =
21:         request.body.instanceId ||
22:         request.body.instance_id ||
23:         request.body.gameId ||
24:         request.body.game_id;
25:       requirePlayerRole(await requireRoomRole(actorId, roomId));
26:
27:       return withRoomIdempotency(
28:         roomId,
29:         request,
30:         "player.mini_game_submit",
31:         () => submitPlayerMiniGame({ roomId, gameId, actorId, answer }),
32:       );
33:     },
34:   );
35:
36:   app.post(
37:     "/api/rooms/:roomId/sections/:sectionId/start",
38:     { schema: completeSectionSchema },
39:     async (request) => {
40:       const actorId = requireActor(request);
41:       const { roomId, sectionId } = request.params;
42:       const roleSlotId = requirePlayerRole(
43:         await requireRoomRole(actorId, roomId),
44:       );
45:       return startPlayerSection({ roomId, roleSlotId, sectionId });
46:     },
47:   );
48:
49:   app.post("/api/rooms/:roomId/sections/:sectionId/complete", { schema: completeSectionSchema }, asyn ...
50:       const actorId = requireActor(request);

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 39 页 =====

01:       const { roomId, sectionId } = request.params;
02:       const roleSlotId = requirePlayerRole(
03:         await requireRoomRole(actorId, roomId),
04:       );
05:       return withRoomIdempotency(roomId, request, "sections.complete", () =>
06:         completePlayerSection({ roomId, roleSlotId, sectionId, actorId }),
07:       );
08:   });
09:
10:   app.post(
11:     "/api/rooms/:roomId/notebook",
12:     { schema: notebookEntrySchema },
13:     async (request, reply) => {
14:       const actorId = requireActor(request);
15:       const { roomId } = request.params;
16:       const roleSlotId = requirePlayerRole(
17:         await requireRoomRole(actorId, roomId),
18:       );
19:       const { sourceType, sourceId, title, body } = request.body;
20:       const entry = await withRoomIdempotency(
21:         roomId,
22:         request,
23:         "player.notebook_create",
24:         () =>
25:           addPlayerNotebookEntry({
26:             roomId,
27:             roleSlotId,
28:             actorId,
29:             sourceType,
30:             sourceId,
31:             title,
32:             body,
33:           }),
34:       );
35:       return reply.code(201).send(entry);
36:     },
37:   );
38:
39:   app.delete(
40:     "/api/rooms/:roomId/notebook/:entryId",
41:     { schema: deleteNotebookEntrySchema },
42:     async (request) => {
43:       const actorId = requireActor(request);
44:       const { roomId, entryId } = request.params;
45:       const roleSlotId = requirePlayerRole(
46:         await requireRoomRole(actorId, roomId),
47:       );
48:       return withRoomIdempotency(
49:         roomId,
50:         request,

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 40 页 =====

01:         "player.notebook_delete",
02:         () => removePlayerNotebookEntry({ roomId, roleSlotId, entryId }),
03:       );
04:     },
05:   );
06:
07:   app.get(
08:     "/api/rooms/:roomId/my-timeline",
09:     { schema: { params: playerProgressRoomIdParams } },
10:     async (request) => {
11:       const actorId = requireActor(request);
12:       const { roomId } = request.params;
13:       const roleSlotId = requirePlayerRole(
14:         await requireRoomRole(actorId, roomId),
15:       );
16:       return getPlayerTimeline({ roomId, roleSlotId, actorId });
17:     },
18:   );
19: }
20:
21: // ===== 文件结束：backend/src/routes/player-progress-routes.js =====
22:
23: // ===== 文件：play/src/runtime/game-action-controller.js =====
24: export async function handlePlayGameAction({
25:   action,
26:   button,
27:   state,
28:   api,
29:   render,
30:   setToast,
31:   formatApiError,
32:   pullRoomData,
33:   handleCompleteSection,
34:   handleCompletePlayerTask,
35:   handleSubmitTestimony,
36:   handleSubmitSatisfaction,
37:   handleReadClue,
38:   handleInvestigate,
39:   handleMiniGameSubmit,
40:   documentRef = document,
41: }) {
42:   switch (action) {
43:     case "complete-section":
44:       await handleCompleteSection(button.dataset.sectionId);
45:       return true;
46:     case "complete-player-task":
47:       await handleCompletePlayerTask(button.dataset.taskId);
48:       return true;
49:     case "submit-testimony":
50:       await handleSubmitTestimony();

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 41 页 =====

01:       return true;
02:     case "submit-satisfaction":
03:       await handleSubmitSatisfaction();
04:       return true;
05:     case "save-suspicion":
06:       await saveSuspicion({
07:         button,
08:         state,
09:         api,
10:         render,
11:         setToast,
12:         formatApiError,
13:         pullRoomData,
14:       });
15:       return true;
16:     case "submit-vote-ballot":
17:       await submitVote({
18:         button,
19:         state,
20:         api,
21:         render,
22:         setToast,
23:         formatApiError,
24:         pullRoomData,
25:       });
26:       return true;
27:     case "submit-mechanism-choice":
28:       await submitMechanismChoice({
29:         button,
30:         state,
31:         api,
32:         render,
33:         setToast,
34:         formatApiError,
35:         pullRoomData,
36:       });
37:       return true;
38:     case "submit-private-action":
39:       await submitPrivateAction({
40:         state,
41:         api,
42:         render,
43:         setToast,
44:         formatApiError,
45:         pullRoomData,
46:         documentRef,
47:       });
48:       return true;
49:     case "read-clue":
50:       await handleReadClue(button.dataset.clueId);

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 42 页 =====

01:       return true;
02:     case "investigate":
03:       await handleInvestigate(button.dataset.pointId);
04:       return true;
05:     case "mini-game-submit":
06:       await handleMiniGameSubmit(button);
07:       return true;
08:     default:
09:       return false;
10:   }
11: }
12:
13: async function submitMechanismChoice({
14:   button,
15:   state,
16:   api,
17:   render,
18:   setToast,
19:   formatApiError,
20:   pullRoomData,
21: }) {
22:   const decisionKey = button.dataset.decisionKey;
23:   const optionKey = button.dataset.optionKey;
24:   const expectedRevision = Number(
25:     state.home?.currentState?.mechanism?.revision || 0,
26:   );
27:   if (!decisionKey || !optionKey || !expectedRevision) return;
28:   try {
29:     await api.submitMechanismDecision(state.roomId, decisionKey, {
30:       optionKey,
31:       expectedRevision,
32:     });
33:     await pullRoomData({ partial: true });
34:     setToast(
35:       button.dataset.submissionMode === "private_choice"
36:         ? "秘密承诺已交给主持人"
37:         : "你的倾向已交给主持人",
38:       render,
39:       { patch: true },
40:     );
41:   } catch (error) {
42:     setToast(formatApiError(error, "倾向提交失败"), render, { patch: true });
43:   }
44: }
45:
46: async function saveSuspicion({
47:   button,
48:   state,
49:   api,
50:   render,

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 43 页 =====

01:   setToast,
02:   formatApiError,
03:   pullRoomData,
04: }) {
05:   const card = button.closest(".suspicion-card");
06:   const level = Number(
07:     card?.querySelector("[data-suspicion-level]")?.value || 0,
08:   );
09:   const reason = card?.querySelector("[data-suspicion-reason]")?.value || "";
10:   try {
11:     await api.setSuspicion(state.roomId, button.dataset.targetRole, {
12:       level,
13:       reason,
14:     });
15:     await pullRoomData({ partial: true });
16:     setToast("怀疑度已保存", render, { patch: true });
17:   } catch (error) {
18:     setToast(formatApiError(error, "保存失败"), render, { patch: true });
19:   }
20: }
21:
22: async function submitVote({
23:   button,
24:   state,
25:   api,
26:   render,
27:   setToast,
28:   formatApiError,
29:   pullRoomData,
30: }) {
31:   const voteId = button.dataset.voteId;
32:   const optionId = button.dataset.optionId;
33:   if (!voteId || !optionId) return;
34:   try {
35:     await api.submitVoteBallot(state.roomId, voteId, { optionId });
36:     await pullRoomData({ partial: true });
37:     setToast("投票已提交", render, { patch: true });
38:   } catch (error) {
39:     setToast(formatApiError(error, "提交失败"), render, { patch: true });
40:   }
41: }
42:
43: async function submitPrivateAction({
44:   state,
45:   api,
46:   render,
47:   setToast,
48:   formatApiError,
49:   pullRoomData,
50:   documentRef,

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 44 页 =====

01: }) {
02:   const titleElement = documentRef.querySelector("[data-private-action-title]");
03:   const bodyElement = documentRef.querySelector("[data-private-action-body]");
04:   const title = titleElement?.value?.trim();
05:   const body = bodyElement?.value?.trim() || "";
06:   const actionType =
07:     documentRef.querySelector("[data-private-action-type]")?.value ||
08:     "ask_host";
09:   if (!title) {
10:     setToast("请填写标题", render, { patch: true });
11:     return;
12:   }
13:   try {
14:     await api.createPrivateAction(state.roomId, { actionType, title, body });
15:     if (titleElement) titleElement.value = "";
16:     if (bodyElement) bodyElement.value = "";
17:     await pullRoomData({ partial: true });
18:     setToast("已提交给主持人", render, { patch: true });
19:   } catch (error) {
20:     setToast(formatApiError(error, "提交失败"), render, { patch: true });
21:   }
22: }
23:
24: // ===== 文件结束：play/src/runtime/game-action-controller.js =====
25:
26: // ===== 文件：play/src/views/game-home-views.js =====
27: import { asArray, escapeHtml } from "../../../shared/security.js";
28: import { currentScene, playerProgress, state } from "../state.js";
29: import { clueIsRead } from "../utils/clues.js";
30: import { renderVoiceCompact } from "./voice.js";
31: import { roomContentBindingPresentation } from "../../../shared/room-content-binding.js";
32: import {
33:   normalizeRuntimeCurrentState,
34:   primaryRuntimeAction,
35: } from "../../../shared/runtime-current-state.js";
36: import {
37:   mechanismInteractionCard,
38:   normalizeMechanismInteraction,
39:   normalizeMechanismOptionPresentation,
40: } from "../../../shared/mechanism-interactions.js";
41:
42: export function renderGameResume() {
43:   return `
44:     <section class="game-resume card">
45:       <p class="eyebrow">恢复对局</p>
46:       <h2>正在进入房间…</h2>
47:       <p class="muted">读取你的角色、分幕与线索进度</p>
48:     </section>`;
49: }
50:

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 45 页 =====

01: function hostNudgeBanner() {
02:   const nudge = state.hostNudge;
03:   if (!nudge?.message) return "";
04:   return `
05:     <div class="banner host-nudge-banner">
06:       <div>
07:         <strong>主持人提醒</strong>
08:         <p>${escapeHtml(nudge.message)}</p>
09:       </div>
10:       <button class="btn quiet compact" type="button" data-action="dismiss-host-nudge">知道了</button>
11:     </div>`;
12: }
13:
14: function hostConfirmBanner() {
15:   const hc = state.home?.hostConfirm;
16:   if (!hc?.pendingCount) return "";
17:   if (hc.waitingForYou) {
18:     const sample = hc.titles?.[0] ? `「${escapeHtml(hc.titles[0])}」` : "";
19:     return `
20:       <div class="banner host-wait-banner">
21:         <strong>等待主持人确认</strong>
22:         <p>${sample}${hc.pendingCount > 1 ? ` 等 ${hc.pendingCount} 条` : ""} — 确认后新分幕/场景会自动解锁。</p>
23:       </div>`;
24:   }
25:   return `
26:     <div class="banner host-wait-banner soft">
27:       <strong>主持人正在处理 ${hc.pendingCount} 条待确认事件</strong>
28:       <p>与你相关的推进会在确认后实时通知。</p>
29:     </div>`;
30: }
31:
32: function roomContentBindingBanner() {
33:   const binding = roomContentBindingPresentation(
34:     state.home?.room?.contentBinding,
35:   );
36:   return `
37:     <div class="banner room-content-binding-banner ${binding.tone === "published" ? "soft" : "host-wa ...
38:       <strong>${escapeHtml(binding.label)}</strong>
39:       <p>${escapeHtml(binding.detail)}</p>
40:     </div>`;
41: }
42:
43: function runtimeStateBanner() {
44:   if (!state.home?.currentState) return "";
45:   const current = normalizeRuntimeCurrentState(state.home?.currentState, {
46:     audience: "player",
47:     connected: state.roomEventsConnected,
48:   });
49:   return `
50:     <div class="banner room-content-binding-banner ${current.syncState.status === "synced" ? "soft" : ...

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 46 页 =====

01:       <div>
02:         <strong>${escapeHtml(current.phase.label)}</strong>
03:         <p>${escapeHtml(current.phase.detail)} · ${current.syncState.status === "synced" ? "进度已同步" :  ...
04:       </div>
05:       <span class="status-chip ${current.syncState.isFrozen ? "published" : "testing"}">${current.syn ...
06:     </div>`;
07: }
08:
09: function renderMechanismDecision(decision) {
10:   const interaction = normalizeMechanismInteraction(decision.interaction);
11:   const card = mechanismInteractionCard(interaction.kind);
12:   const selectedOptionKey = decision.submission?.optionKey || "";
13:   const privateSubmission = interaction.submissionMode === "private_choice";
14:   const deadlineAt = decision.deadlineAt
15:     ? new Date(decision.deadlineAt).getTime()
16:     : Number.NaN;
17:   const expired = Number.isFinite(deadlineAt) && Date.now() >= deadlineAt;
18:   const deadlineLabel = Number.isFinite(deadlineAt)
19:     ? new Date(deadlineAt).toLocaleTimeString("zh-CN", {
20:         hour: "2-digit",
21:         minute: "2-digit",
22:         second: "2-digit",
23:       })
24:     : `${Math.ceil(interaction.deadlineSeconds / 60)} 分钟`;
25:   return `<div class="mechanism-decision mechanism-kind-${escapeHtml(interaction.kind)}">
26:     <div class="mechanism-decision-head"><span class="mechanism-kind-label is-${escapeHtml(card.theme ...
27:     ${interaction.deadlineSeconds ? `<div class="mechanism-deadline ${expired ? "is-expired" : ""}">< ...
28:     <div class="mechanism-option-list">${asArray(decision.options)
29:       .map((option, index) => {
30:         const presentation = normalizeMechanismOptionPresentation(
31:           option.presentation,
32:         );
33:         const selected = selectedOptionKey === option.key;
34:         const isDefault = interaction.defaultOptionKey === option.key;
35:         return `<button type="button" class="mechanism-option-card ${selected ? "is-selected" : ""}"  ...
36:         <span>${escapeHtml(presentation.eyebrow || `${card.shortLabel} ${String(index + 1).padStart(2 ...
37:         <strong>${escapeHtml(option.choiceText)}</strong>
38:         ${isDefault ? `<small class="mechanism-default-label">超时默认方案</small>` : ""}
39:         ${presentation.publicPreview ? `<p>${escapeHtml(presentation.publicPreview)}</p>` : ""}
40:         ${presentation.costLabel || presentation.riskLabel || presentation.sequenceLabel ? `<small>${ ...
41:         <em>${selected ? (privateSubmission ? "秘密承诺已提交" : "已提交此倾向") : privateSubmission ? "秘密提交承诺" :  ...
42:       </button>`;
43:       })
44:       .join("")}</div>
45:     <p class="mechanism-submission-help">${
46:       privateSubmission
47:         ? "你的承诺只对本人和主持人可见；其他玩家不会看到你的选择。"
48:         : "你的提交不会立即改写剧情；主持人会看到全桌倾向并确认最终结算。"
49:     }</p>
50:   </div>`;

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 47 页 =====

01: }
02:
03: export function renderMechanismProgress() {
04:   const mechanism = state.home?.currentState?.mechanism;
05:   if (!mechanism) return "";
06:   if (mechanism.stale) {
07:     return `
08:       <article class="mechanism-progress card is-waiting">
09:         <p class="eyebrow">实时剧情机制</p>
10:         <h3>等待主持人同步新版本</h3>
11:         <p class="muted">当前机制与房间内容版本不一致，旧状态不会继续影响本场运行。</p>
12:       </article>`;
13:   }
14:   if (!mechanism.initialized) {
15:     return `
16:       <article class="mechanism-progress card is-waiting">
17:         <p class="eyebrow">实时剧情机制</p>
18:         <h3>等待主持人开启</h3>
19:         <p class="muted">创作者设计的机制已随剧本进入房间，主持人开启后会自动显示当前轮次。</p>
20:       </article>`;
21:   }
22:   if (mechanism.status === "completed") {
23:     return `
24:       <article class="mechanism-progress card is-complete">
25:         <p class="eyebrow">实时剧情机制 · 已完成</p>
26:         <h3>${escapeHtml(mechanism.ending?.title || "本场机制已结算")}</h3>
27:         <p class="muted">最终状态已由主持端确认并同步。</p>
28:       </article>`;
29:   }
30:
31:   const round = mechanism.currentRound;
32:   if (!round) return "";
33:   const decisions = asArray(mechanism.decisions);
34:   return `
35:     <article class="mechanism-progress card">
36:       <div class="mechanism-progress-head">
37:         <div>
38:           <p class="eyebrow">实时剧情机制 · 第 ${Number(round.sequence) || 1} / ${Number(mechanism.totalRoun ...
39:           <h3>${escapeHtml(round.title || "当前轮次")}</h3>
40:         </div>
41:         <span class="status-chip testing">主持端结算</span>
42:       </div>
43:       ${round.goal ? `<p class="mechanism-goal">${escapeHtml(round.goal)}</p>` : ""}
44:       ${round.playerAction ? `<div class="mechanism-player-action"><span>你们现在要做</span><strong>${escap ...
45:       ${decisions.map(renderMechanismDecision).join("")}
46:       <p class="muted small">选择由全桌讨论，主持人结算后会自动同步结果与下一轮。</p>
47:     </article>`;
48: }
49:
50: function renderRoomMembers() {

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 48 页 =====

01:   const members = state.home?.roomMembers || [];
02:   if (!members.length) return "";
03:   return `
04:     <article class="card members-card">
05:       <div class="section-head"><h3>房间成员</h3><p>${members.filter((m) => m.online).length} 人已选角色</p></div>
06:       <div class="member-list">
07:         ${members
08:           .map(
09:             (member) => `
10:           <div class="member-row ${member.online ? "is-online" : ""}">
11:             <span class="member-avatar">${escapeHtml(String(member.role_name?.[0] || "?"))}</span>
12:             <div>
13:               <strong>${escapeHtml(member.role_name)}</strong>
14:               <span>${member.display_name ? escapeHtml(member.display_name) : member.online ? "已加入" : ...
15:             </div>
16:           </div>`,
17:           )
18:           .join("")}
19:       </div>
20:     </article>`;
21: }
22:
23: export function renderRoomMembersHtml() {
24:   return renderRoomMembers();
25: }
26:
27: export function renderGameSidebar() {
28:   const role = state.home?.role;
29:   return `
30:         <article class="role-card-side card">
31:           <p class="eyebrow">你的角色</p>
32:           <h2>${escapeHtml(role?.name || "未选择")}</h2>
33:           <p>${escapeHtml(role?.private_profile || role?.public_profile || "暂无角色资料")}</p>
34:         </article>
35:         ${renderRoomMembers()}
36:         <div class="sidebar-actions">
37:           <button class="btn quiet full" type="button" data-action="leave-room">离开房间</button>
38:         </div>`;
39: }
40:
41: export function renderGameHome() {
42:   const home = state.home;
43:   const progress = playerProgress(home);
44:   const scene = currentScene(state.exploration);
45:   const role = home?.role;
46:   const next = progress.nextSection;
47:
48:   return `
49:     <div class="home-dashboard">
50:       ${roomContentBindingBanner()}

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 49 页 =====

01:       ${runtimeStateBanner()}
02:       ${renderMechanismProgress()}
03:       ${renderVoiceCompact()}
04:       <article class="player-hero card live-flash">
05:         <div class="player-hero-copy">
06:           <p class="eyebrow">${escapeHtml(role?.name || "你的角色")} · 当前场景</p>
07:           <h2>${escapeHtml(scene.title)}</h2>
08:           <p>${escapeHtml(scene.text)}</p>
09:         </div>
10:         <div class="scene-art" aria-hidden="true">${escapeHtml(scene.art)}</div>
11:       </article>
12:
13:       <div class="stat-grid">
14:         <article class="stat-card"><span>分幕进度</span><strong>${progress.sectionsCompleted} / ${progres ...
15:         <article class="stat-card"><span>我的线索</span><strong>${progress.clueCount}</strong></article>
16:         <article class="stat-card"><span>共享线索</span><strong>${progress.sharedClueCount}</strong></art ...
17:         <article class="stat-card"><span>背包物品</span><strong>${progress.inventoryCount}</strong></article>
18:       </div>
19:
20:       ${renderPlayerActionsHub(home, next)}
21:
22:       ${
23:         (state.exploration?.scenes?.length || 0) > 0
24:           ? `
25:         <button class="btn outline" type="button" data-action="switch-tab" data-tab="explore">前往探索场景（ ...
26:           : ""
27:       }
28:     </div>`;
29: }
30:
31: function renderPlayerActionsHub(home, nextSection) {
32:   const sections = home?.sections || [];
33:   const tasks = asArray(home?.tasks);
34:   const clues = home?.clues || [];
35:   const sharedClues = home?.sharedClues || [];
36:   const scenes = state.exploration?.scenes || [];
37:   const points = scenes.flatMap((s) =>
38:     (s.investigation_points || []).map((p) => ({ ...p, sceneName: s.name })),
39:   );
40:   const pending = home?.hostConfirm;
41:   const currentGame = state.currentGame;
42:   const votes = asArray(home?.activeVotes);
43:
44:   const pendingTasks = tasks.filter((task) => task.status !== "completed");
45:   const openVotes = votes.filter(
46:     (vote) => vote.status === "open" && !vote.submitted_at,
47:   );
48:   const unreadSections = sections.filter((s) => !s.completed);
49:   const unreadClues = clues.filter((c) => !clueIsRead(c, { owned: true }));
50:   const unreadShared = sharedClues.filter(

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 50 页 =====

01:     (c) => !clueIsRead(c, { owned: false }),
02:   );
03:   const unreadAllClues = [...unreadClues, ...unreadShared];
04:   const availablePoints = points.filter(
05:     (p) => !p.investigated && p.hasRequiredItem,
06:   );
07:   const blockedPoints = points.filter(
08:     (p) => !p.investigated && !p.hasRequiredItem && p.requiredItemName,
09:   );
10:
11:   let primary;
12:   if (pendingTasks.length) {
13:     primary = {
14:       title: pendingTasks[0].body || "完成本幕任务",
15:       detail: pendingTasks[0].tips || "先处理未完成任务，再推进调查和讨论。",
16:       action: "switch-tab",
17:       data: 'data-tab="tasks"',
18:       button: "处理任务",
19:     };
20:   } else if (openVotes.length) {
21:     primary = {
22:       title: openVotes[0].title || "参与投票 / 指认",
23:       detail: openVotes[0].prompt || "主持人已开启投票，请先完成你的选择。",
24:       action: "switch-tab",
25:       data: 'data-tab="social"',
26:       button: "去投票",
27:     };
28:   } else if (pending?.waitingForYou) {
29:     primary = {
30:       title: "剧情推进等待主持确认",
31:       detail: "你已经触发关键节点。确认后新内容会自动刷新。",
32:       action: "switch-tab",
33:       data: 'data-tab="voice"',
34:       button: "进入讨论",
35:     };
36:   } else if (unreadAllClues.length) {
37:     primary = {
38:       title: `阅读线索：${unreadAllClues[0].name}`,
39:       detail: "标记已读后可补充解读、公开或私享给指定玩家。",
40:       action: "switch-tab",
41:       data: 'data-tab="clues"',
42:       button: "查看线索",
43:     };
44:   } else if (nextSection && !nextSection.completed) {
45:     primary = {
46:       title: nextSection.title || "阅读当前分幕",
47:       detail: `第 ${nextSection.sequence} 幕 · 尚未读完`,
48:       action: "goto-section",
49:       data: `data-section-id="${escapeHtml(nextSection.id)}"`,
50:       button: "继续阅读",

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 51 页 =====

01:     };
02:   } else if (availablePoints.length) {
03:     primary = {
04:       title: `调查：${availablePoints[0].name}`,
05:       detail: `地点：${availablePoints[0].sceneName || "当前场景"}`,
06:       action: "switch-tab",
07:       data: 'data-tab="explore"',
08:       button: "去探索",
09:     };
10:   } else {
11:     primary = {
12:       title: "整理线索或进入语音讨论",
13:       detail: "当前没有必须完成的动作",
14:       action: "switch-tab",
15:       data: 'data-tab="voice"',
16:       button: "讨论",
17:     };
18:   }
19:
20:   const serverAction = primaryRuntimeAction(home?.currentState);
21:   if (serverAction) {
22:     const targetTab = new Set([
23:       "home",
24:       "sections",
25:       "social",
26:       "explore",
27:       "voice",
28:       "clues",
29:       "tasks",
30:     ]).has(serverAction.target)
31:       ? serverAction.target
32:       : "home";
33:     primary = {
34:       title: serverAction.label,
35:       detail: serverAction.reason,
36:       action: "switch-tab",
37:       data: `data-tab="${escapeHtml(targetTab)}"`,
38:       button: "去处理",
39:     };
40:   }
41:
42:   const readItems = [
43:     ...pendingTasks.slice(0, 2).map((t) => ({
44:       label: "未完成任务",
45:       title: t.body,
46:       action: "switch-tab",
47:       data: 'data-tab="tasks"',
48:     })),
49:     ...openVotes.slice(0, 2).map((v) => ({
50:       label: "待投票",

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 52 页 =====

01:       title: v.title,
02:       action: "switch-tab",
03:       data: 'data-tab="social"',
04:     })),
05:     ...unreadSections.slice(0, 3).map((s) => ({
06:       label: "未读分幕",
07:       title: s.title || `第 ${s.sequence} 幕`,
08:       action: "goto-section",
09:       data: `data-section-id="${escapeHtml(s.id)}"`,
10:     })),
11:     ...unreadClues.slice(0, 2).map((c) => ({
12:       label: "未读线索",
13:       title: c.name,
14:       action: "switch-tab",
15:       data: 'data-tab="clues"',
16:     })),
17:     ...unreadShared.slice(0, 2).map((c) => ({
18:       label: "未读共享",
19:       title: c.name,
20:       action: "switch-tab",
21:       data: 'data-tab="clues"',
22:     })),
23:   ];
24:   const exploreItems = [
25:     ...availablePoints.slice(0, 3).map((p) => ({
26:       label: `可调查 · ${p.sceneName || ""}`,
27:       title: p.name,
28:       action: "switch-tab",
29:       data: 'data-tab="explore"',
30:     })),
31:     ...blockedPoints.slice(0, 2).map((p) => ({
32:       label: `需要 ${p.requiredItemName || "物品"}`,
33:       title: p.name,
34:       action: "switch-tab",
35:       data: 'data-tab="explore"',
36:     })),
37:   ];
38:   const waitItems = [];
39:   if (pending?.pendingCount && !pending.waitingForYou)
40:     waitItems.push({
41:       label: "确认后自动推送",
42:       title: `主持人处理 ${pending.pendingCount} 条待确认`,
43:     });
44:   if (currentGame && currentGame.status !== "success")
45:     waitItems.push({ label: "解密机关", title: "有待解决的数字锁机关" });
46:   if (!scenes.length)
47:     waitItems.push({ label: "完成阅读后解锁", title: "等待主持人开放场景" });
48:   if (
49:     !unreadSections.length &&
50:     !availablePoints.length &&

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 53 页 =====

01:     !pending?.pendingCount
02:   )
03:     waitItems.push({ label: "可自由行动", title: "当前无紧急待办" });
04:
05:   const renderItem = (item) =>
06:     item.action
07:       ? `<button class="action-list-item" type="button" data-action="${escapeHtml(item.action)}" ${it ...
08:       : `<div class="action-list-item is-static"><span>${escapeHtml(item.label)}</span><b>${escapeHtm ...
09:
10:   return `
11:     <article class="next-action card">
12:       <div class="next-action-primary">
13:         <div>
14:           <p class="eyebrow">建议下一步</p>
15:           <h3>${escapeHtml(primary.title)}</h3>
16:           <p class="muted">${escapeHtml(primary.detail)}</p>
17:         </div>
18:         <button class="btn primary" type="button" data-action="${escapeHtml(primary.action)}" ${prima ...
19:       </div>
20:       <div class="next-action-lists">
21:         <div class="action-col">
22:           <p class="action-col-label">📖 读什么</p>
23:           ${readItems.length ? readItems.map(renderItem).join("") : '<p class="muted small">暂无未读内容</p>'}
24:         </div>
25:         <div class="action-col">
26:           <p class="action-col-label">🔍 查什么</p>
27:           ${exploreItems.length ? exploreItems.map(renderItem).join("") : '<p class="muted small">当前无 ...
28:         </div>
29:         <div class="action-col">
30:           <p class="action-col-label">⏳ 等什么</p>
31:           ${waitItems.length ? waitItems.map(renderItem).join("") : '<p class="muted small">暂无等待项</p>'}
32:         </div>
33:       </div>
34:     </article>`;
35: }
36:
37: export function renderHostConfirmBannerHtml() {
38:   return hostNudgeBanner() + hostConfirmBanner();
39: }
40:
41: // ===== 文件结束：play/src/views/game-home-views.js =====
42:
43: // ===== 文件：host/src/runtime/host-mechanism-controller.js =====
44: import { formatApiError } from "../errors.js";
45: import { state } from "../state.js";
46: import {
47:   initializeHostMechanismRuntime,
48:   loadHostMechanismRuntime,
49:   submitHostMechanismAction,
50: } from "./host-mechanism-service.js";

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 54 页 =====

01:
02: export function createHostMechanismController({ render, showToast }) {
03:   async function run(label, task) {
04:     if (state.hostMechanismBusy) return;
05:     state.hostMechanismBusy = label;
06:     state.hostMechanismError = "";
07:     render();
08:     try {
09:       const result = await task();
10:       if (result) showToast(`${label}完成，机制状态已核对`);
11:     } catch (error) {
12:       state.hostMechanismError = formatApiError(error, `${label}失败`);
13:       showToast(state.hostMechanismError);
14:     } finally {
15:       state.hostMechanismBusy = "";
16:       render();
17:     }
18:   }
19:
20:   async function handleAction(action, element) {
21:     if (action === "host-mechanism-refresh") {
22:       await run("刷新机制运行态", () => loadHostMechanismRuntime());
23:       return true;
24:     }
25:     if (action === "host-mechanism-initialize") {
26:       await run("初始化机制", () => initializeHostMechanismRuntime());
27:       return true;
28:     }
29:     if (action === "host-mechanism-decision") {
30:       await run("结算玩家选择", () =>
31:         submitHostMechanismAction({
32:           type: "decision",
33:           decisionKey: element?.dataset?.decisionKey || "",
34:           optionKey: element?.dataset?.optionKey || "",
35:         }),
36:       );
37:       return true;
38:     }
39:     if (action === "host-mechanism-deadline-default") {
40:       await run("执行超时默认方案", () =>
41:         submitHostMechanismAction({
42:           type: "decision",
43:           source: "deadline_default",
44:           decisionKey: element?.dataset?.decisionKey || "",
45:           optionKey: element?.dataset?.optionKey || "",
46:         }),
47:       );
48:       return true;
49:     }
50:     if (action === "host-mechanism-investigation") {

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 55 页 =====

01:       await run("结算调查", () =>
02:         submitHostMechanismAction({
03:           type: "investigation",
04:           investigationKey: element?.dataset?.investigationKey || "",
05:           outcome:
06:             element?.dataset?.outcome === "failure" ? "failure" : "success",
07:         }),
08:       );
09:       return true;
10:     }
11:     if (action === "host-mechanism-advance") {
12:       await run("推进机制轮次", () =>
13:         submitHostMechanismAction({ type: "advance" }),
14:       );
15:       return true;
16:     }
17:     return false;
18:   }
19:
20:   return { handleAction };
21: }
22:
23: // ===== 文件结束：host/src/runtime/host-mechanism-controller.js =====
24:
25: // ===== 文件：host/src/views/host-mechanism-workspace.js =====
26: import { escapeHtml } from "../utils/format.js";
27: import { state } from "../state.js";
28: import {
29:   mechanismKeyLabel,
30:   mechanismValueLabel,
31:   normalizeHostMechanismRuntime,
32: } from "../runtime/host-mechanism-model.js";
33: import {
34:   mechanismInteractionCard,
35:   normalizeMechanismInteraction,
36:   normalizeMechanismOptionPresentation,
37: } from "../../../shared/mechanism-interactions.js";
38:
39: function metricRows(rows, emptyText) {
40:   if (!rows.length)
41:     return `<div class="host-mechanism-empty">${escapeHtml(emptyText)}</div>`;
42:   return rows
43:     .map(
44:       (row) => `<div class="host-mechanism-metric">
45:     <span>${escapeHtml(row.label)}</span><strong>${escapeHtml(mechanismValueLabel(row.value))}</strong>
46:     <small>${escapeHtml(row.key)}</small>
47:   </div>`,
48:     )
49:     .join("");
50: }

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 56 页 =====

01:
02: export function mechanismDeadlineStatus(
03:   roundStartedAt,
04:   deadlineSeconds,
05:   now = Date.now(),
06: ) {
07:   if (!roundStartedAt) return null;
08:   const startedAt = new Date(roundStartedAt).getTime();
09:   const seconds = Number(deadlineSeconds) || 0;
10:   if (!Number.isFinite(startedAt) || seconds <= 0) return null;
11:   const deadlineMs = startedAt + seconds * 1000;
12:   const remainingSeconds = Math.max(
13:     0,
14:     Math.ceil((deadlineMs - Number(now)) / 1000),
15:   );
16:   return {
17:     deadlineAt: new Date(deadlineMs).toISOString(),
18:     expired: remainingSeconds === 0,
19:     remainingSeconds,
20:     label:
21:       remainingSeconds === 0
22:         ? "已到期"
23:         : `剩余 ${Math.ceil(remainingSeconds / 60)} 分钟`,
24:   };
25: }
26:
27: function decisionRows(model, busy) {
28:   if (!model.decisions.length)
29:     return `<div class="host-mechanism-empty">当前轮次没有待结算选择，可以核对后推进。</div>`;
30:   return model.decisions
31:     .map((decision) => {
32:       const interaction = normalizeMechanismInteraction(decision.interaction);
33:       const card = mechanismInteractionCard(interaction.kind);
34:       const resource = interaction.resourceKey
35:         ? model.resources.find((item) => item.key === interaction.resourceKey)
36:         : null;
37:       const deadline = mechanismDeadlineStatus(
38:         model.roundStartedAt,
39:         interaction.deadlineSeconds,
40:       );
41:       const defaultOption = decision.options.find(
42:         (option) => option.key === interaction.defaultOptionKey,
43:       );
44:       const submissions = model.submissionSummary.find(
45:         (entry) => entry.decisionKey === decision.key,
46:       ) || { total: 0, options: [], roles: [] };
47:       const privateSubmission = interaction.submissionMode === "private_choice";
48:       const optionDisabled = busy || Boolean(deadline?.expired);
49:       return `<article class="host-mechanism-action-card mechanism-kind-${escapeHtml(interaction.kind)}">
50:       <div class="host-mechanism-action-head"><span class="mechanism-kind-chip is-${escapeHtml(card.t ...

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 57 页 =====

01:       ${
02:         interaction.deadlineSeconds || resource
03:           ? `<div class="host-mechanism-constraints">
04:         ${interaction.deadlineSeconds ? `<span>时限 <b>${deadline ? escapeHtml(deadline.label) : `${Mat ...
05:         ${resource ? `<span>${escapeHtml(resource.label)} <b>${escapeHtml(mechanismValueLabel(resourc ...
06:       </div>`
07:           : ""
08:       }
09:       <div class="host-mechanism-options">${(decision.options || [])
10:         .map((option, index) => {
11:           const presentation = normalizeMechanismOptionPresentation(
12:             option.presentation,
13:           );
14:           const preferenceCount =
15:             submissions.options.find((entry) => entry.optionKey === option.key)
16:               ?.count ?? 0;
17:           const roleNames = submissions.roles
18:             .filter((entry) => entry.optionKey === option.key)
19:             .map((entry) => entry.roleName)
20:             .filter(Boolean);
21:           return `<button type="button" class="host-mechanism-option" data-action="host-mechanism-dec ...
22:           <span>${escapeHtml(presentation.eyebrow || `${card.shortLabel} ${String(index + 1).padStart ...
23:           <strong>${escapeHtml(option.choiceText || mechanismKeyLabel(option.key))}</strong>
24:           ${presentation.publicPreview ? `<small>${escapeHtml(presentation.publicPreview)}</small>` : ""}
25:           ${presentation.costLabel || presentation.riskLabel || presentation.sequenceLabel ? `<em>${[ ...
26:           <small class="host-mechanism-preference">玩家倾向 ${preferenceCount}${roleNames.length ? ` · ${ ...
27:         </button>`;
28:         })
29:         .join("")}</div>
30:       <p class="host-mechanism-submission-note">${
31:         privateSubmission
32:           ? `已收到 ${submissions.total} 份秘密承诺；仅主持人可见承诺人与内容，其他玩家不会看到。`
33:           : `已收到 ${submissions.total} 份玩家倾向；倾向仅供参考，仍由主持人确认最终结算。`
34:       }</p>
35:       ${
36:         interaction.kind === "timed_crisis"
37:           ? defaultOption
38:             ? `<div class="host-mechanism-deadline-default"><div><strong>超时默认：${escapeHtml(defaultOpt ...
39:             : `<p class="host-mechanism-deadline-warning">本限时决策尚未配置超时默认方案，请返回创作者端补齐后再运行。</p>`
40:           : ""
41:       }
42:     </article>`;
43:     })
44:     .join("");
45: }
46:
47: function investigationRows(model, busy) {
48:   if (!model.investigations.length)
49:     return `<div class="host-mechanism-empty">当前轮次没有待执行调查。</div>`;
50:   return model.investigations

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 58 页 =====

01:     .map(
02:       (item) => `<article class="host-mechanism-action-card">
03:     <div><span class="cloud-pill">调查行动</span><h4>${escapeHtml(item.action || mechanismKeyLabel(item.k ...
04:     <div class="host-mechanism-options">
05:       <button type="button" class="primary-btn" data-action="host-mechanism-investigation" data-inves ...
06:       ${item.failure ? `<button type="button" class="secondary-btn" data-action="host-mechanism-inves ...
07:     </div>
08:   </article>`,
09:     )
10:     .join("");
11: }
12:
13: function latestChanges(model) {
14:   if (!model.latestChanges.length)
15:     return `<div class="host-mechanism-empty">尚无状态变更记录。</div>`;
16:   return model.latestChanges
17:     .slice(0, 8)
18:     .map(
19:       (change) => `<div class="host-mechanism-change">
20:     <span>${escapeHtml(change.targetType || "状态")}</span><b>${escapeHtml(mechanismKeyLabel(change.tar ...
21:     <small>${escapeHtml(mechanismValueLabel(change.before))} → ${escapeHtml(mechanismValueLabel(chang ...
22:   </div>`,
23:     )
24:     .join("");
25: }
26:
27: function endingProspects(model) {
28:   if (!model.endingProspects.length) return "";
29:   return `<section class="host-mechanism-prospects"><div class="section-head compact"><div><h4>结局前景</ ...
30:     <div class="host-mechanism-prospect-list">${model.endingProspects
31:       .map(
32:         (
33:           route,
34:         ) => `<article class="host-mechanism-prospect ${route.reachable ? "is-reachable" : "is-closed"}">
35:       <div><span class="status-chip ${route.reachable ? "published" : "draft"}">${route.reachable ? " ...
36:       <p>${
37:         route.unmetRequirements?.length
38:           ? route.unmetRequirements
39:               .map(
40:                 (item) =>
41:                   `${mechanismKeyLabel(item.targetKey)}：当前 ${mechanismValueLabel(item.current)}，目标 ${ ...
42:               )
43:               .map(escapeHtml)
44:               .join("<br>")
45:           : "当前条件已经满足；仍需完成剩余轮次后正式结算。"
46:       }</p>
47:       <small>${escapeHtml(route.key)}</small>
48:     </article>`,
49:       )
50:       .join("")}</div>

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 59 页 =====

01:   </section>`;
02: }
03:
04: export function renderHostMechanismWorkspace() {
05:   const model = normalizeHostMechanismRuntime(state.cloudHostMechanismRuntime);
06:   const busy = Boolean(state.hostMechanismBusy);
07:   const header = `<div class="section-head compact">
08:     <div><p class="section-kicker">MECHANISM RUNTIME</p><h3>机制运行</h3><p>按作品机制包结算选择、调查、资源与证据；所有操作都会记录版 ...
09:     <button type="button" class="secondary-btn" data-action="host-mechanism-refresh" ${busy ? "disabl ...
10:   </div>`;
11:
12:   if (!model.loaded)
13:     return `<section class="card host-mechanism-workspace">${header}<div class="empty-state">正在等待机制运行 ...
14:   if (!model.initialized) {
15:     const missingPackage = model.errorCode === "MECHANISM_PACKAGE_NOT_FOUND";
16:     return `<section class="card host-mechanism-workspace">${header}
17:       <div class="host-mechanism-onboarding ${model.error ? "is-warning" : ""}">
18:         <span class="host-mechanism-mark">${missingPackage ? "—" : "01"}</span>
19:         <div><h4>${missingPackage ? "当前剧本还没有可执行机制包" : model.error ? "机制运行态暂时不可用" : "本房间尚未初始化机制"}</h4> ...
20:         ${model.error || missingPackage ? "" : `<button type="button" class="primary-btn" data-action ...
21:       </div>
22:     </section>`;
23:   }
24:
25:   return `<section class="card host-mechanism-workspace ${model.stale ? "is-stale" : ""}">
26:     ${header}
27:     ${model.stale ? `<div class="host-mechanism-warning"><b>内容绑定已变化，当前运行态停止结算。</b><span>请先建立检查点并由高级恢复 ...
28:     ${state.hostMechanismError ? `<div class="host-mechanism-warning"><b>上一操作未完成</b><span>${escapeHtm ...
29:     <div class="host-mechanism-hero">
30:       <div><span class="cloud-pill">${model.status === "completed" ? "已完成" : `第 ${model.roundSequence ...
31:       <div class="host-mechanism-revision"><span>运行版本</span><strong>R${model.revision}</strong><small ...
32:     </div>
33:     <div class="host-mechanism-grid">
34:       <section><h4>状态账本</h4><div class="host-mechanism-metrics">${metricRows(model.states, "本作没有登记状态变 ...
35:       <section><h4>题材资源</h4><div class="host-mechanism-metrics">${metricRows(model.resources, "本作没有登记 ...
36:       <section><h4>证据状态</h4><div class="host-mechanism-metrics">${metricRows(model.evidence, "本作没有登记证 ...
37:     </div>
38:     ${endingProspects(model)}
39:     ${
40:       model.status === "completed"
41:         ? `<div class="host-mechanism-ending"><span>命中结局</span><strong>${escapeHtml(mechanismKeyLabel ...
42:         : `<div class="host-mechanism-action-grid"><section><h4>待决选择</h4>${decisionRows(model, busy | ...
43:     <div class="host-mechanism-footer"><div><h4>最近变化</h4>${latestChanges(model)}</div><button type="b ...
44:     }
45:   </section>`;
46: }
47:
48: // ===== 文件结束：host/src/views/host-mechanism-workspace.js =====
49:
50: // ===== 文件：backend/src/routes/host-mechanism-runtime-routes.js =====

===== 织幕长线剧本杀自动化叙事与运营平台软件 V1.0 源程序鉴别材料 后30页 第 60 页 =====

01: import { withRoomIdempotency } from "../idempotency-helpers.js";
02: import { requireActor } from "../request-actor.js";
03: import {
04:   executeRoomMechanismAction,
05:   getRoomMechanismRuntime,
06:   initializeRoomMechanismRuntime
07: } from "../room-mechanism-runtime-service.js";
08: import { requireHostMembership } from "./host-route-guards.js";
09: import {
10:   hostMechanismRuntimeActionSchema,
11:   hostMechanismRuntimeGetSchema,
12:   hostMechanismRuntimeInitializeSchema
13: } from "./schemas/host-mechanism-runtime.js";
14:
15: export async function registerHostMechanismRuntimeRoutes(app) {
16:   app.get("/api/rooms/:roomId/host/mechanism-runtime", {
17:     schema: hostMechanismRuntimeGetSchema
18:   }, async (request) => {
19:     const actorId = requireActor(request);
20:     const { roomId } = request.params;
21:     await requireHostMembership(actorId, roomId);
22:     return getRoomMechanismRuntime({ roomId, ...request.query });
23:   });
24:
25:   app.post("/api/rooms/:roomId/host/mechanism-runtime/initialize", {
26:     schema: hostMechanismRuntimeInitializeSchema
27:   }, async (request, reply) => {
28:     const actorId = requireActor(request);
29:     const { roomId } = request.params;
30:     await requireHostMembership(actorId, roomId);
31:     const response = await withRoomIdempotency(roomId, request, "host.mechanism.initialize", () => (
32:       initializeRoomMechanismRuntime({ roomId, actorId, ...(request.body ?? {}) })
33:     ));
34:     return reply.code(response.replayed ? 200 : 201).send(response);
35:   });
36:
37:   app.post("/api/rooms/:roomId/host/mechanism-runtime/actions", {
38:     schema: hostMechanismRuntimeActionSchema
39:   }, async (request) => {
40:     const actorId = requireActor(request);
41:     const { roomId } = request.params;
42:     await requireHostMembership(actorId, roomId);
43:     return withRoomIdempotency(roomId, request, "host.mechanism.action", () => (
44:       executeRoomMechanismAction({ roomId, actorId, ...request.body })
45:     ));
46:   });
47: }
48:
49: // ===== 文件结束：backend/src/routes/host-mechanism-runtime-routes.js =====
50:
