#!/usr/bin/env node
/**
 * @deprecated 2026-06 — 已改为单服务 fullstack（deploy/Dockerfile.fullstack）。
 * 请使用: npm run railway:deploy
 */
console.error("railway-deploy-web: 已弃用。请使用 npm run railway:deploy（单服务 fullstack）。");
process.exit(1);
