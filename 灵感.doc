考虑加入一个智能客服，然后接api，把我们整个三端口的世界书写进去，可以方便进行指引，代替初始流程指引
英文像segment这种的得有中文化，而且这些概念都有点太中二了
角色私人剧情这些破按钮得根据我们现有的后端重新写，有点太多了，而且还和别的地方重复
至今没想到这个真相链和断言到底跟什么有关？在哪体现作用，核诡也太奇怪了的术语，角色关系图添加完居然不能删？感觉整个真相与关系有点游离整个内容了
剧情图谱还是不错，但是为什么这个界面好丑，搞好看一点吧得，而且尺寸怎么自适应呢？
线索流程图也是这个拖拽画布做的也很奇怪，即丑又不全，失败
自动化规则，新建规则没联动起来，可以小游戏怎么样的，这些判断条件还是不全而且拖拽不了滚轮不了，看不全都，这种中间傻不拉几弹窗实在是太丑了
小游戏设计太少了，这个可以参考一些经典的剧本，设计小游戏？要不要单独做一个设计页面呢？
至今没设计过存档于复盘，完全不知道长什么样子，
账号与内容资产加载出现两层页面这也是个大问题，而且加载时间很长
前端这些只是一小部分，后端的话重点不再是按页面检查，而是按系统不变量检查：
用户 A 永远不能读取或修改用户 B 的世界。
房间 A 的事件永远不能进入房间 B。
玩家永远不能看到未公开内容和其他角色秘密。
已提交的业务不能因为通知失败向用户返回失败。
重试不能重复发线索、重复计分或重复推进剧情。
断线重连后不能漏事件，也不能重复执行事件。
回滚不能出现代码回滚了、数据库却无法兼容。
管理、调试、导入和内部接口不能绕过正式权限。
任意失败都应该可追踪、可恢复、可解释。
最值得优先审计的系统性风险
P0：认证不等于授权
登录正常并不能证明安全。必须逐路由检查：
是否验证 token。
是否验证世界所有权。
是否验证房间成员身份。
是否验证 Host、Creator、Player 角色。
是否验证资源真的属于 URL 中的世界或房间。
是否只依赖前端隐藏按钮。
是否存在通过修改 ID 访问其他用户资源的 IDOR。
SSE 订阅是否可能监听其他房间。
游客和匿名用户的边界是否一致。
这通常是同类产品最危险的漏洞区。
P0：私人内容泄露
织幕的数据本身高度敏感。角色秘密、隐藏线索、主持答案和真相 Bible 必须逐层检查：
查询时是否已过滤，而不是返回前端后再隐藏。
API 错误信息是否泄露隐藏内容。
搜索、导出、版本快照和复盘是否绕过可见性。
SSE 事件 payload 是否携带超出玩家权限的数据。
缓存键是否包含用户、房间和角色维度。
切换角色后是否残留上一个角色的数据。
功能正常时，这类漏洞通常完全看不出来。
P0：幂等和事务边界
需要故意制造：
用户连续点击两次。
浏览器超时后自动重试。
API 已写库但响应中断。
Outbox 写入成功但通知失败。
Host 和玩家同时操作。
SSE 断线期间发生多个事件。
两个 Host 同时确认同一个事件。
然后检查是否出现重复线索、重复积分、重复结算、章节倒退或事件顺序错误。
P0：后台入口与“后门”
真正意义上的恶意后门未必常见，但工程项目中经常存在等价风险：
没有保护的管理接口。
开发环境留下的 debug route。
硬编码 token、默认密码或测试账号。
根据环境变量关闭认证。
任意 SQL、动态代码执行、eval、child_process。
未限制的文件上传、路径拼接和导入功能。
SSRF、XSS、HTML 注入和不安全反序列化。
CORS、Cookie、JWT 和代理头配置错误。
日志记录密码、token、私人剧本或完整请求体。
依赖包安装脚本和供应链风险。
不能承诺“审计一次就证明不存在后门”，但可以通过代码来源、锁文件、依赖清单、敏感信息扫描、危险调用扫描和权限矩阵，把风险降到可解释范围。
P1：迁移和恢复
快速重构最容易出现“新安装正常，旧环境升级失败”。
必须分别验证：
全新数据库从零迁移。
接近生产状态的旧数据库逐步升级。
重复运行迁移。
中途失败后重新执行。
应用版本回滚但数据库不回滚。
备份恢复到隔离环境。
恢复后认证、房间事件和私人内容仍然一致。
“数据库有备份”不等于“项目可以恢复”。只有恢复演练成功才算证据。
P1：可用性和资源耗尽
系统性故障不一定来自黑客，也可能来自正常用户：
SSE 连接持续增长。
LISTEN 连接占满连接池。
慢查询导致其他请求排队。
大型世界导入耗尽内存。
高频轮询造成数据库放大。
某个房间的事件积压拖慢所有房间。
日志无限增长占满磁盘。
外部 AI 或语音服务异常拖垮主流程。
需要验证超时、限流、并发上限、连接释放和降级行为。能否在干净环境下一次启动？
依赖、环境变量和数据库要求是否明确？
模块边界是否真实存在，还是只拆了文件？
是否有统一认证和授权入口？
核心业务不变量在哪里保证？
测试是否覆盖失败路径，而不只是成功路径？
数据库迁移能否安全运行和恢复？
日志能否定位一次用户操作经过了哪些系统？
发布失败后多久能够回滚？
新开发者修改一个事件，会不会同时破坏三端？
文档描述是否与当前代码一致？
有没有一块代码只有原作者敢改？
真正影响评价的不是“代码是不是足够前沿”，而是系统行为是否可预测。
建议建立一份 RC-0 系统审计包
下一轮最好不要边审边大规模重构。先只读审计，形成证据，再统一修复。
最终产物应包括：
系统数据流和信任边界图。
全量 API 的认证、授权、资源归属矩阵。
Creator、Host、Player 的数据可见性矩阵。
调试入口、管理入口和危险调用清单。
密钥、依赖和供应链扫描结果。
数据库事务、Outbox、Journal 和幂等风险清单。
SSE 断线、重放、重复和乱序测试矩阵。
新建、升级、失败重试和恢复演练报告。
性能与资源耗尽风险。
外部开发者接管性评估。
P0/P1/P2 缺陷清单。
最终的“可封闭测试 / 可公开测试 / 不建议上线”结论。